# 키워드북 설계 가이드 (Keyword Book Architecture & Modular Sub-Prompts)

다섯 번째 산출물인 `build/keyword-book.md`는 크랙 플랫폼의 **키워드북(Keyword Book)** 탭과 **단축어(Shortcuts)** 탭에 등록할 항목들을 정의합니다.

상시 시스템 프롬프트(7,000자 상한)의 예산을 아끼면서, 특정 상황(키스, 19+ 성애, 특수 전투, 정치 암투 등)이 발생했을 때 **3슬롯 런타임 주입**을 통해 연출 밀도를 극대화하는 **가변형 모듈식 시스템 프롬프트(Modular Sub-System Prompt)**의 핵심 문서입니다.

---

## 목차
- [1. 키워드북의 본질: 단순 정보 나열 vs 가변형 메인 프롬프트](#1-키워드북의-본질-단순-정보-나열-vs-가변형-메인-프롬프트)
- [2. 런타임 호출 테크닉: 1턴 브릿징과 큐 투척 (Turn Bridging)](#2-런타임-호출-테크닉-1턴-브릿징과-큐-투척-turn-bridging)
- [3. 3슬롯 런타임 메커니즘과 설계 원칙](#3-3슬롯-런타임-메커니즘과-설계-원칙)
- [4. 키워드북 극압축 방법론: 400자 초과 설계 ➡️ 표/기호 압축 & 메인 기호 공유](#4-키워드북-극압축-방법론-400자-초과-설계-️-표기호-압축--메인-기호-공유)
- [5. 초경량 직관형 작성 규격 (크랙 UI 1:1 매핑)](#5-초경량-직관형-작성-규격-크랙-ui-11-매핑)
- [6. 핵심 연출 트리거 모듈 레시피 (선택형 형식 템플릿)](#6-핵심-연출-트리거-모듈-레시피-선택형-형식-템플릿)
  - [모듈 1: 성애 및 18금 성행위 연출 (`kb.mature` / `성애_19금`)](#모듈-1-성애-및-18금-성행위-연출-kbmature--성애_19금--unsafe-최상단-필수)
  - [모듈 2: 인지저해 및 치환 기준표 (`kb.perception` / `인지_치환`)](#모듈-2-인지저해-및-치환-기준표-kbperception--인지_치환)
  - [모듈 3: 사고·손해 및 복구비용 기준 (`kb.damage` / `사고_변상`)](#모듈-3-사고손해-및-복구비용-기준-kbdamage--사고_변상)
  - [모듈 4: 2인 1항목 페어링 압축 (`kb.pair` / `인물페어_압축`)](#모듈-4-2인-1항목-페어링-압축-kbpair--인물페어_압축)
- [7. 키워드북 설계 실전 프로 팁 8대 원칙 (Pro Tips)](#7-키워드북-설계-실전-프로-팁-8대-원칙-pro-tips)
- [8. 단축어는 같은 파일에 살지만 다른 문서가 소유한다](#8-단축어는-같은-파일에-살지만-다른-문서가-소유한다)
- [9. SAFE vs UNSAFE 키워드북 분리 작성 원칙](#9-safe-vs-unsafe-키워드북-분리-작성-원칙-keyword-book-safemd--keyword-book-unsafemd)

---

## 1. 키워드북의 본질: 단순 정보 나열 vs 가변형 메인 프롬프트

키워드북은 단순한 세계관 설정집이나 인물 사전(Lore Encyclopedia)으로만 쓸 수도 있지만, 실전에서는 **"상황에 따라 상시 프롬프트에 동적으로 끼워 넣는 가변형 메인 프롬프트(Modular Sub-System Prompt)"**로 설계할 때 가장 강력한 위력을 발휘합니다.

| 접근 방식 | 단순 정보 나열형 (기본형) | 가변형 메인 프롬프트형 (실전 고밀도) |
|---|---|---|
| **성격** | 세계관/인물 설정, 역사, 사실 관계 기록 | 특정 상황 전용 런타임 연출 지침 및 행동 규칙 |
| **작성 내용** | "누구는 어떤 마법을 쓰고 성격이 어떻다" | 카메라 앵글, 대사 톤, 감각적 신체 묘사, 공방 규칙, 허용/금지령 |
| **적합한 상황** | 배경 지식, 고유 명사 설명, 서브 팩트 | 전투 결전, 19+ 성애 씬, 스킨십, 극적 밀실 대화 |

> 💡 **설계 유연성**: 모든 항목을 가변 프롬프트로 짤 필요는 없습니다. 단순 세계관 용어는 설정 설명형으로, 극적인 연출 변화가 필요한 씬은 가변형 프롬프트로 상황에 맞게 섞어서 구성할 수 있습니다.
>
> ⚡ **런타임 동작 원리**: [crack-internal-runtime.md](crack-internal-runtime.md) 분석에 따르면, 키워드북은 유저가 입력한 최신 대사 바로 밑(`## Additional Information & Rules` ➡️ `<knowledge_base>`)에 실시간 주입됩니다. LLM은 입력 프롬프트의 맨 끝(최하단)에 있는 지침을 가장 강력하게 따르므로(Recency Bias), 메인 프롬프트보다 키워드북 지침의 구속력이 훨씬 강합니다!

---

## 2. 런타임 호출 테크닉: 1턴 브릿징과 큐 투척 (Turn Bridging)

크랙은 플레이어나 AI가 해당 키워드를 텍스트로 발화했을 때 키워드북을 로드합니다. 플레이어가 우연히 키워드를 입력하길 마냥 기다리면 원하는 타이밍에 연출 모듈이 발동하지 않을 수 있습니다.

이를 해결하기 위해 **"메인 프롬프트에서 1턴을 벌어주고, 키워드북 호출 단어(큐)를 뱉는" 1턴 브릿징 테크닉**을 적용할 수 있습니다.

```
[Turn N : 전조 및 브릿징 턴]
- 메인 프롬프트: 즉시 결판/본 씬으로 급발진하지 않고, 대치/분위기 조성을 서술하며 1턴을 번다.
- 키워드 큐 투척: 나레이션이나 NPC 대사 속에 키워드북의 트리거 단어(Cue)를 자연스럽게 발화한다.
  (예: "칼자루를 쥐며 '진검 결투'를 선언한다", "침실 문을 걸어 잠그며 숨을 고른다")

              ▼ (대화 컨텍스트에 키워드 큐가 기록됨)

[Turn N+1 : 키워드북 로드 & 연출 폭발 턴]
- 크랙 엔진: 직전 턴에 노출된 키워드를 감지하여 해당 키워드북을 시스템 최상단에 주입!
- 키워드북: 주입된 가변형 프롬프트 지침(카메라 구도, 체위/공방 규칙, 신체 감각)에 따라 본 씬 연출 수행!
```

---

## 3. 3슬롯 런타임 메커니즘과 설계 원칙

1. **상시 프롬프트 vs 키워드북 분리**:
   - **상시 메인 프롬프트**: 상시 HUD 기호, 기본 6중 잠금, 보편적 대사 톤 등 항구적인 규칙.
   - **키워드북**: 특정 사건, 특수 페어 만남, 19+ 씬, 특수 결투 등 **상황별로 호출되는 가변 모듈**.
2. **동시 활성화 최대 3개 한도 & 상단 우선순위**:
   - 크랙 엔진은 턴당 최대 3개의 키워드북 항목만 주입합니다. 4개 이상 매칭 시 **목록 하단에 있는 항목부터 드랍(Drop)**됩니다.
   - ⚠️ **성인/특수 연출 모듈 최상단 배치 필수**: 성인(19+) 모듈이나 특수 전투 결전 모듈은 그 턴에서 절대로 드랍되어서는 안 되는 최고 중요도 지침입니다. 일반 인물 설정이나 장소 로어가 상단에 있으면 정작 중요한 성애 모듈이 3슬롯 밖으로 밀려 누락되는 대참사가 발생합니다. 따라서 **`성애_19금` 및 특수 연출 모듈은 반드시 키워드북 목록의 최상단(1~3순위)에 배치**합니다.
3. **항목당 400자 고밀도 충원 (권장 350~395자)**:
   - 크랙 UI 한도(400자) 안에서 필요한 설정과 연출 규칙을 집약적으로 구성합니다.
4. **주입 위치가 곧 주의 집중도**:
   - 키워드북은 시스템 프롬프트 안이 아니라 **유저의 가장 최근 메시지 바로 뒤**(`## Additional Information & Rules` ➡️ `<knowledge_base>`)에 주입됩니다. 출력에 가장 가까운 자리이므로, 같은 문장이라도 통합 프롬프트에 있을 때보다 강하게 먹습니다. 전체 파이프라인은 [crack-internal-runtime.md](crack-internal-runtime.md) 참조.
   - 그래서 배치 선택은 **위치(키워드북이 강함) vs 확실성(통합 프롬프트가 매턴 보장)**의 거래입니다. 매 턴 반드시 지켜져야 하는 규칙은 통합 프롬프트, 그 턴에 강하게 밀어붙여야 하는 연출은 키워드북.

---

## 4. 키워드북 극압축 방법론: 400자 초과 설계 ➡️ 표/기호 압축 & 메인 기호 공유

키워드북 항목 하나하나는 크랙의 귀중한 3개 동적 슬롯 중 하나를 차지합니다. 처음부터 400자 제한에 맞춰 소극적으로 생각하면 정보량이 빈약해져 1슬롯의 가치를 살리지 못합니다.

### 4.1 핵심 설계 원칙: 400자 초과 기획 ➡️ 표·기호 고밀도 압축
1. **1단계: 풍부한 원형 설계 (500~700자 상당)**:
   - 처음에는 400자를 넘길 만큼 상황의 인과, 수치 기준, 대사 톤, 신체 감각, 제약 사항을 깊이 있게 설계합니다.
2. **2단계: 메인 프롬프트 기호 체계 100% 공유 (Shared Syntax)**:
   - 크랙 런타임에서 키워드북은 메인 프롬프트와 동일한 LLM 컨텍스트(`## Additional Information & Rules` ➡️ `<knowledge_base>`)로 통합 평가됩니다.
   - 따라서 **메인 프롬프트 `# 표기`에서 선언한 공통 기호(`ⓤ`, `ⓒ`, `→`, `·`, `/`, `｜`, `×`, `↑/↓` 등)를 키워드북에서도 그대로 공유**합니다.
   - 키워드북 본문 안에서 "플레이어는", "NPC는", "~하면 ~한다" 같은 긴 주어와 연결사를 중복 설명하지 않고 기호로 대체합니다.
3. **3단계: 표(Table)와 치환 기호를 통한 400자 한도 패킹 (≤400자)**:
   - 줄글 문장 대신 **치환표(`A→B`), 수치 기준표, 슬롯 구분자(`｜`), 슬래시(`/`)**를 동원해 400자 이내(권장 350~395자)로 압축합니다.

### 4.2 메인 프롬프트와 공유하는 공통 압축 기호
| 기호 | 의미 / 용법 | 키워드북 실전 활용 예시 |
|:---:|---|---|
| `ⓤ` | 플레이어(User) | `ⓤ 동의 전 유도만, 거절·침묵 시 즉시 물러남` |
| `ⓒ` | 대상 인물 / NPC | `ⓒ는 현재 단계까지만 시도`, `현장 ⓒ 둘 이상 모이면` |
| `→` | 조건·결과 / 인과 / 일상 치환 | `충격 굉음→피해 범위 확인→굳어버린 침묵`, `날개·후광→큰 등산가방·조명 역광` |
| `·` | 동시 속성 / 병렬 묶음 | `대사·접촉·호흡에 연결`, `임대 250만·공과 80만·알바 220만` |
| `/` | 대안 / 항목 구분 / 택일 | `a01정상위/a02기승위/a03후배위...`, `반응/응징` |
| `｜` | 슬롯 칸 구분자 | `설정: 에리(호문쿨루스｜정밀측정)·세라(연금술사｜만성피로)` |
| `×` | 금지 / 미보유 / 제외 | `남성·미보유×`, `초자연적 돈 해결 기적×`, `새 장면 이어쓰기×` |
| `↑ / ↓` | 수치/상태의 상승 및 하락 | `의심 수치↑`, `체류 시간↓` |

### 4.3 실전 압축 변환 사례 (Before & After)

* **압축 전 (서술형 원본 기획; 530자 상당 — 400자 한도 초과로 등록 불가)**:
  > 이 세계관에는 인지저해라는 현상이 있어서 일반 인간들은 천사나 악마 같은 초자연적 존재의 외형을 목격하더라도 일상적인 상식에 맞춰 왜곡해서 기억합니다. 예를 들어 날개나 후광을 보면 큰 등산가방이나 조명 역광으로 착각하고, 뿔은 특이한 머리띠로, 꼬리는 길게 늘어뜨린 벨트 끈으로, 여우귀는 헤어밴드로, 세로동공은 컬러렌즈로 봅니다. 반투명한 발끝은 바닥 반사광으로 치환됩니다. 초자연적 존재들은 자신의 외형을 굳이 숨기지 않으며, 일반인들의 상식이 깨지는 것은 비행이나 마법, 괴력으로 기물을 파괴하는 등의 물리적 사건이 터졌을 때뿐입니다. 만약 사건이 발생하면 치환이 무너지고 관리 부서가 개입하여 은폐 수습에 들어갑니다. 일반인 관찰자는 이상 현상이 반복될수록 의심이 깊어지고 질문이 길어집니다. 플레이어가 실체를 알고 있다는 사실을 눈치채면 단골들은 신기해하거나 약점을 잡힌 것처럼 경계하게 됩니다.

* **압축 후 (치환표 및 메인 기호 체계 적용; 366자 — `convenience/build` 실전 형식)**:
  ```markdown
  치환표: 날개·후광→큰 등산가방·조명 역광, 뿔→특이한 머리띠, 꼬리→길게 늘어뜨린 벨트끈, 여우귀→헤어밴드, 세로동공→컬러렌즈, 반투명 발끝→바닥 반사광.
  인간 대중은 초자연적 외형을 목격해도 일상 상식에 맞추어 자동 왜곡·치환해 기억한다. 초자연 존재는 외형을 숨기지 않으며, 깨지는 것은 외형이 아닌 사건(비행·마법·괴력 파괴)이다.
  사건 발생 시 치환이 붕괴되어 각 조직의 은폐 수습이 개입한다. 한지수는 아직 필터가 있어 억지 납득하나 이상 현상이 반복될수록 치환이 지연되고 질문이 길어진다.
  점장이 실체를 본다는 것을 알아챈 단골들은 신기해하거나 약점을 잡힌 듯 경계한다.
  ```

---

## 5. 초경량 직관형 작성 규격 (크랙 UI 1:1 매핑)

크랙 웹 UI에 그대로 복사-붙여넣기할 수 있는 3대 요소로 간결하게 작성합니다.

```markdown
## [항목 이름]
- 키워드: 키워드1, 키워드2, 키워드3
- 내용:
[크랙 UI 본문 칸에 넣을 문자열; 350~395자 권장]
```

### 💡 크랙 UI 1:1 매핑
1. **항목 이름 (`## 제목`)**: 크랙 키워드북 항목 제목 칸에 입력
2. **트리거 키워드 (`- 키워드:`)**: 키워드 입력 칸에 등록 (1~5개, 쉼표 구분)
3. **내용 (`- 내용:`)**: 본문 칸에 붙여넣기 (≤400자 한도)

---

## 6. 핵심 연출 트리거 모듈 레시피 (선택형 형식 템플릿)

> 💡 **작성 원칙**: 구구절절한 소설 서술을 배제하고, 모델의 연출 행동을 강제하는 **규칙·수치·치환표·지침 뼈대(Skeleton)** 위주로 400자 이내 작성합니다. 메인 프롬프트의 기호(`ⓤ`, `ⓒ`, `→`, `·`, `/`, `｜`, `×`)를 적극 활용합니다.

### 모듈 1: 성애 및 18금 성행위 연출 (`kb.mature` / `성애_19금`) — UNSAFE 최상단 필수
```markdown
# `kb.mature` — 성애 및 18금 성행위 연출
- activation_setting: 전체 시작 설정
- activation_when: 사용자 메시지 또는 모델 출력에 키워드가 감지될 때
- keywords: [🔞]

## Entry text
성인 이미지=UNSAFE 한정, 행위·체위 전환 직전(대사 유무 무관) `![이름]({IMG}/{인물번호}/{코드}.webp)` 1장. 대상01~[N]만: a01정상위/a02기승위/a03후배위/a04들박/a05핸드잡/a06펠라/a07딥쓰롯/a08파이즈리/a09커널링구스/a11목키스/a13프렌치키스/a14필로우토크/a15유혹. 미보유 인물은 호출×. ⓤ 동의 전 유도만, 거절·침묵 시 즉시 물러남. 행동 나열 금지, 욕망·신뢰·망설임·주도권 변화를 대사·접촉·호흡에 연결. 신체 감각·체온·여운 반영, 장면 전후 관계·감정 갱신. (≤400자)
```

---

### 모듈 2: 인지저해 및 치환 기준표 (`kb.perception` / `인지_치환`)
```markdown
# `kb.perception` — 인지저해와 일상 치환 기준
- activation_setting: 전체 시작 설정
- activation_when: 사용자 메시지 또는 모델 출력에 키워드가 감지될 때
- keywords: [인지저해, 치환, 목격, 들킴, 노출]

## Entry text
치환표: [초자연외형A]→[일상물건/착시A], [초자연외형B]→[일상물건B], [이능력C]→[상식적원인C].
일반 대중은 비일상적 존재/외형을 목격해도 일상 상식에 맞추어 자동 왜곡·치환해 기억한다. 비일상 존재는 외형을 숨기지 않으며, 깨지는 것은 외형이 아닌 사건([대규모파괴]·[초자연현상])이다.
사건 발생 시 치환이 붕괴되어 [관리조직/수습부서]의 은폐 수습이 개입한다. 일반인 관찰자는 이상 현상이 반복될수록 치환이 지연되고 의심이 깊어진다. ⓤ가 실체를 안다는 사실이 드러나면 대상은 경계하거나 흥미를 보인다. (≤400자)
```

---

### 모듈 3: 사고·손해 및 복구비용 기준 (`kb.damage` / `사고_변상`)
```markdown
# `kb.damage` — 사고와 변상 기준
- activation_setting: 전체 시작 설정
- activation_when: 사용자 메시지 또는 모델 출력에 키워드가 감지될 때
- keywords: [파손, 변상, 보상, 손해, 수리]

## Entry text
파손 연출: 충격 굉음→피해 범위 확인→굳어버린 침묵 순.
복구 비용 기준: [핵심설비A 정지] [200만 원], [외벽/유리문 파손] [80만 원], [진열대/기물 완파] [30만 원]. 영업 1시간 중단은 [일일 순익]을 증발시킨다.
일일 수입이 [기준금액] 안팎이라 단 한 건의 사고도 며칠치 흑자를 날려버린다.
변상 주체: 조직 소속([세력A]·[세력B])은 결재 서류 핑계로 처리가 지연되고, 무소속은 현금이나 물건으로 때우려 한다. 아무도 안 물면 ⓤ가 뒤집어쓴다. 변상 협상은 장부와 관계 중 하나를 반드시 바꾼다. (≤400자)
```

---

### 모듈 4: 2인 1항목 페어링 압축 (`kb.pair` / `인물페어_압축`)
```markdown
# `kb.pair_ab` — [인물A]와 [인물B]
- activation_setting: 전체 시작 설정
- activation_when: 사용자 메시지 또는 모델 출력에 키워드가 감지될 때
- keywords: [[인물A], [인물B], [소속/관계키워드]]

## Entry text
설정: [인물A]([나이/종족]·[직책/역할]·[핵심성격]·[능력/특징]). [인물B]([나이/종족]·[직책/역할]·[핵심성격]·[능력/특징]).
[인물A]: [말투 특징과 대표 대사톤] ("[인물B/ⓤ 대상 한마디]"). [특정 조건/자극 시 반응 변화].
[인물B]: [말투 특징과 대표 대사톤] ("[인물A/ⓤ 대상 한마디]"). [특정 조건/자극 시 반응 변화].
[인물A]가 [시비/행동]을 걸고 [인물B]가 [반응/응징]하며 현장을 뒤흔드는 상호작용 티키타카. 둘이 만나면 ⓤ 개입 없이도 자체적인 서사 소동이 발생함. (200자×2=≤400자)
```

---

## 7. 키워드북 설계 실전 프로 팁 11대 원칙 (Pro Tips)

수많은 스토리챗 제작자들이 겪은 시행착오(슬롯 낭비, 환각, 모듈 미발동)에서 도출된 핵심 설계 팁입니다. 세 갈래로 묶입니다.

| 갈래 | 팁 |
|---|---|
| **무엇을 넣나** | 1(정적 vs 동적), 3(우선 모듈 최상단), 5(압축), 10(본문은 델타만), 11(HUD 수치는 넣지 마라) |
| **언제 뜨나 (트리거)** | 2(브릿징), 4(3슬롯 교통정리), 6(체인 리액션), 8(하단 상시 항목), 9(출력 규약 토큰 재사용) |
| **어떻게 묶나** | 7(페어링과 씬 인원 상한) |

### 팁 1: 정적 사전(Lore)과 동적 가변 모듈(Directives)의 분리
- **단순 백과사전형**: 지명, 마법 학파, 가문 역사 등은 사실 관계 설명 위주로 가볍게 작성합니다.
- **가변 프롬프트형**: 결투, 19+ 합방, 스킨십, 밀실 대화 등 극적 씬은 단순 설명이 아니라 모델의 행동을 제어하는 **시스템 지시문(`-카메라:`, `-호흡:`, `-금지/허용:`)** 형태로 작성하여 해당 턴의 뇌(지침)를 완전히 교체합니다.

### 팁 2: 1턴 브릿징 & 큐 투척 (Turn Bridging)
- 플레이어가 우연히 키워드를 입력해 주길 기다리지 마세요.
- **대사/나레이션 큐 투척**: 메인 프롬프트가 전조를 묘사하며 **1턴을 벌고, 나레이션이나 NPC 대사 속에 키워드북 호출 단어(예: '진검 결투', '합방', '침실')를 자연스럽게 뱉어두면**, 다음 턴에 크랙 엔진이 키워드를 100% 감지하여 해당 연출 모듈을 최상단에 주입합니다.
- **상태창(HUD) 앵커링 큐 (HUD Date/State Trigger)**:
  - 타임라인 이벤트나 특정 분기 이벤트의 경우, **상태창에 출력되는 특정 날짜(`2025.1.1`, `5.4`)나 상태 문자(`[비상사태]`, `[안식]`)를 키워드로 등록**해 둘 수 있습니다.
  - 시간 경과나 씬 전환으로 상태창에 해당 날짜/상태가 찍히는 순간, 크랙 엔진이 이를 트리거로 인식해 `이벤트 키워드북(이혼 통보, 생일 전야, 특수 위기)`을 오차 없이 자동으로 로드시킵니다.
  - ⚠️ 단, 상태창에 **영구히 남는** 문자열을 트리거로 쓰면 그 턴 이후로 계속 발동합니다(팁 4). 날짜처럼 넘어가면 사라지는 값만 안전합니다.
- **키워드북은 등장을 만들지 못합니다**: 키워드북은 인물을 *등장시키는* 장치가 아니라 **등장한 뒤 살을 붙이는** 장치입니다. 항목 본문의 `※등장:` 같은 줄은 그 항목이 이미 로드된 다음 턴부터만 먹습니다. 누가 언제 나타나는지(로테이션 규칙, 장소별 상주 인물)는 **상시 프롬프트 소유**입니다. 이 둘을 헷갈리면 "등장 규칙을 키워드북에 적었는데 아무도 안 나온다"가 됩니다 — 그 인물이 등장하지 않았으니 트리거도 안 걸리는 순환입니다.

### 팁 3: 성인/특수 연출 모듈의 키워드북 격리 및 최상단(1순위) 배치
- **메인 프롬프트 격리**: 성인 씬 번호(A코드, 19~56번 등)는 메인 시스템 프롬프트에 절대 적지 않고 오직 키워드북 해당 모듈에만 적습니다. (7,000자 상시 예산 보존 및 일상 대화 중 이미지 누출 방지)
- **키워드북 목록 최상단 배치 필수**: 크랙 엔진은 키워드북 목록을 위에서부터 아래로 읽으며 3슬롯을 채웁니다. 만약 성애 모듈이 목록 하단에 있으면, 인물이나 장소 키워드가 먼저 슬롯을 차지하여 정작 19+ 상황에서 성애 모듈이 드랍(Drop)되는 치명적인 오류가 발생합니다. **따라서 `성애_19금` 및 특수 결전 모듈은 무조건 키워드북 등록표의 최상단(1~3번)에 배치하여 3슬롯 중 1순위로 확정 로드되도록 보장**해야 합니다.

### 팁 4: 3슬롯 교통정리 & 상시 발동 트리거의 대가
- 크랙은 한 턴에 최대 3개 키워드북만 주입하며, 4개 이상 매칭 시 **목록 하단 항목부터** 드랍(Drop)됩니다.
- **사고로 상시 발동시키지 마세요**: 상태창(HUD) 고정 항목명(`[관계]`, `등급`, `상태`)을 트리거로 쓰면 매 턴 발동해 3슬롯 중 1개가 영구 낭비됩니다. `crack-emu lint`가 `always_on_trigger` **error**로 잡습니다.
- **컴파일 후 `Info` 블록 × 전체 키워드 교차검사를 반드시 돌립니다.** HUD 템플릿에 찍히는 모든 문자열과 등록된 모든 키워드를 대조합니다. 눈으로는 안 보입니다 — 실제로 걸린 세 가지:

| 걸린 것 | 왜 |
|---|---|
| `[상황]` 슬롯의 상태 문구를 키워드로 등록 | 그 상태가 켜져 있는 동안 매 턴 발동 |
| `[관계]` 슬롯의 단계 이모지(`💋`)를 키워드로 등록 | 스킨십 단계가 한 번 찍히면 **끝까지 래치**. 되돌아가지 않음 |
| `[상황]` 끝의 `🔞` pre-arm | 의도한 선행 무장이어도 래치됨. **씬 종료 즉시 표식을 내리는 문구**가 본문에 없으면 영구 점유 |

  래치되는 값(관계 단계, 획득 상태)과 지나가는 값(날짜, 장소)은 다릅니다. 트리거로 쓸 수 있는 건 후자뿐입니다.
- **키워드는 2~3개.** 1~5개가 허용된다고 5개를 채우면 오발동만 늘어납니다. `밀착`·`격려`·`스킨십` 같은 범용 명사는 엉뚱한 씬에서 튀어 슬롯을 먹습니다. **발동 의도가 명확한 것만** 넣습니다.
- **의도적 상시 항목은 팁 8의 조건 아래에서만** 만듭니다. 사고와 설계의 차이는 등록 위치와 본문의 폐기 가능성입니다.
- 한 씬에서 동시 발동될 확률이 높은 키워드는 최대 2개까지만 설계하고, 1개 슬롯은 시스템 여유 버퍼로 남겨두는 것이 안전합니다.

### 팁 5: 400자 초과 기획 ➡️ 표·기호 고밀도 압축 (권장 350~395자)
- 처음부터 400자 제한에 갇혀 소극적으로 기획하면 귀중한 1슬롯의 정보 밀도를 낭비하게 됩니다. 500~700자로 과잉 기획한 뒤 350~395자로 압축합니다.
- 압축 루프의 4단계, 전역 공유 기호표, 산출물별 글자 수 타겟은 [semantic-compression.md](semantic-compression.md)가 소유합니다. 숫자를 고칠 일이 있으면 그쪽만 고칩니다.

### 팁 6: 체인 리액션 (연쇄 트리거 바톤 터치)
- 한 모듈이 끝날 때 다음 단계 모듈의 키워드를 대사/나레이션에 흘려주면 자연스럽게 서사의 다음 국면으로 바톤을 넘길 수 있습니다.
  - 예: `스킨십 모듈(💋)`의 마지막 서술에서 "둘만의 침실로 향한다" ➡️ 다음 턴 `합방 모듈(🔞)` 자동 로드!

### 팁 7: 캐릭터 상세설명 오프로딩 & 2인 1항목(200자×2) 페어링 압축
- **메인 프롬프트 1턴 버티기**: 등장인물이 많은 대작의 경우, 메인 프롬프트에는 캐릭터가 첫 등장했을 때 딱 1턴만 버텨줄 수 있는 **최소 정보(외형, 직책, 첫 대사 말투 등 1줄 씨앗)**만 둡니다.
- **심층 설정 키워드북 이관**: 캐릭터가 등장해 대화가 시작되면 이름이 컨텍스트에 찍히므로, 심층 과거사/역린/복잡한 능력 등 상세 설명은 키워드북으로 분리합니다.

**3슬롯은 항목 개수 문제가 아니라 동시 점등 수 문제입니다.** 항목이 40개여도 한 씬에서 2개만 켜지면 아무 문제 없고, 항목이 6개여도 4개가 같이 켜지면 무너집니다. 그래서 묶는 기준은 **조직도가 아니라 실제 co-occurrence** — 같은 부서·같은 직급이 아니라 **실제로 같은 방에 함께 있는 2인**입니다.

- **2인이 최적**: 1인 항목은 슬롯 낭비(한 명 때문에 슬롯 하나를 씀), 4인 항목은 400자를 4등분해 연출이 뭉개집니다. 200자 × 2가 균형점입니다. 키워드 칸에 두 사람 이름을 모두 등록하면 한 명만 등장해도 로드되고, 둘이 함께 나오는 티키타카 씬에서도 **슬롯 1개만** 씁니다.
- **3인 팀은 항상 1명이 남습니다** → 부서를 깨고 **장소 기반 크로스팀 콤비**로 흡수합니다. 예: 마도개발의 내구성 테스트 담당 + 영업의 물류 운반 담당 → "테스트실·물류창고 현장 콤비". 소속은 다르지만 실제로 같은 공간에서 마주치는 빈도가 같은 부서원보다 높습니다.
- **페어링은 씬 인원 상한과 세트로만 작동합니다.** 상시 프롬프트에 `1씬 발화ⓒ≤3인 · 고정 콤비 우선 · 2조까지 혼합` 을 걸어야 씬당 점등이 2슬롯으로 수렴하고, 연출 지침용 1슬롯이 남습니다. 상한이 없으면 아무리 잘 묶어도 5인이 한꺼번에 등장하는 씬에서 무너집니다.


### 팁 8: 하단 상시 강화 항목 (Always-On Reinforcer)

문체, 서술 리듬, 출력 형식처럼 **매 턴 적용되지만 한 턴 빠져도 무너지지 않는** 규칙은, 통합 프롬프트 대신 키워드북 최하단에 상시 발동 항목으로 둘 수 있습니다. §3-4의 위치 이점(유저 메시지 바로 뒤 주입)을 매 턴 얻으면서, 3슬롯 경쟁에서는 가장 먼저 양보하는 자리입니다.

**성립 조건 4개 — 하나라도 어기면 팁 4가 금지하는 슬롯 낭비가 됩니다.**

| 조건 | 이유 |
|---|---|
| **목록 최하단 등록** | 하단 = 제일 먼저 드랍. 중요 모듈(19+·특수 결전)이 슬롯을 뺏기지 않음. `report`가 `high_match_entry_registered_high` warn으로 상단 등록을 잡습니다 |
| **최대 2개** | 3슬롯 중 2개를 상시로 채우면 실질 여유가 0이 됨 |
| **본문이 폐기 가능할 것** | 그 항목이 안 뜬 턴에도 응답이 정상이어야 함. 없으면 무너지는 규칙은 통합 프롬프트 소유 |
| **HUD 고정 항목명이 아닌 트리거** | HUD 라벨은 `always_on_trigger` error |

**트리거 선택.** 1글자(`다`, `어`)는 발동은 보장되지만 한국어 거의 모든 어절 안에 묻혀 `keyword_substring_hazard`에 걸리고, 의도치 않은 다른 항목까지 흔듭니다. 서술문 종결 어미 2글자 이상(`했다`, `였다`, `본다`)이 발동률은 비슷하면서 사고 범위가 좁습니다. 구두점(`.`) 매칭 여부는 **미확인**이므로 트리거로 쓰지 않습니다.

**쓰기 좋은 것**: 문체 패치 블록([prose-style-guide.md](prose-style-guide.md) §5), 서술 리듬 재강화, 반복 문형 차단.
**쓰면 안 되는 것**: 6중 잠금, 출력 계약, HUD 규격, 세계관 코어 — 한 턴이라도 빠지면 즉시 깨지므로 통합 프롬프트 소유입니다.

**검증**: 실제 턴을 돌려 확인합니다. `crack-emu replay` 후 `report`로 발동률과 `slot_overflow`를 보고, 이 항목 때문에 밀려난 항목이 무엇인지 확인합니다. 항목 하나씩 들여다봐서는 드러나지 않습니다.


### 팁 9: 출력 규약 토큰을 트리거로 재사용 (오발동 제거)

인물 이름을 그냥 키워드로 등록하면 지문·회상·HUD·다른 인물의 대사 속 언급에도 전부 걸립니다. 대신 **출력 계약이 이미 강제하는 구분자를 트리거에 포함**시킵니다.

대사 규약이 `**이름**| "대사"` 라면, 키워드를 `이름` 이 아니라 **`**라임**|`** 으로 등록합니다. 이 문자열은 그 인물이 **실제로 발화하는 블록에서만** 생성되므로, 언급과 발화가 정확히 구분됩니다. 회상 속 "라임"이나 남이 말한 "라임"에는 걸리지 않습니다.

**대가는 1턴 지연**입니다. 키워드는 모델이 그 블록을 출력한 *다음* 턴에 감지되므로, 첫 등장 턴은 상시 프롬프트의 인물 명부 1줄 씨앗으로 버텨야 합니다(팁 7). 이 구조를 쓰려면 명부가 반드시 상시 프롬프트에 있어야 합니다.

전제: 출력 계약의 대사 구분자가 실제로 고정되어 있어야 합니다([output-contract.md](output-contract.md)). 구분자를 바꾸면 이 트리거들이 전부 죽습니다.

### 팁 10: 본문은 델타만 — 상시 프롬프트에 있는 것을 다시 쓰지 마라

나이·종족·직급·외모·MBTI·기본 말투는 인물 명부가 **이미 상시 로드 중**입니다. 키워드북 항목에 다시 적는 건 순수 낭비이고, 두 곳이 어긋나면 모델이 어느 쪽을 믿을지 모릅니다.

항목에는 **명부에 없는 것만** 넣습니다:

| 넣을 것 | 넣지 말 것 |
|---|---|
| 단계별 연출 지침(`[초]` / `[친밀]` / `[19+]`) | 나이·종족·직급 |
| 약점·비밀·역린 | 외모 기본 정보 |
| 2인 간 케미와 역학 | MBTI·기본 말투 |
| 등장 이후 스테이징 큐 | 명부에 이미 있는 관계 라벨 |

이 원칙만 지키면 400자 안에 **2인 상세가 여유 있게** 들어갑니다(실측 263~307자).

**2인 케미 한 줄이 개별 설명 두 줄보다 값싸고 강합니다.** "A가 지른 계약을 B가 조항 단위로 붙잡는 브레이크 관계" — 45자로 두 인물의 행동 원리와 씬 구조가 동시에 섭니다. 각자를 따로 설명하면 120자를 쓰고도 둘이 만났을 때 어떻게 되는지는 모릅니다.

### 팁 11: HUD에 매 턴 찍히는 수치의 산정 규칙은 키워드북에 두지 마라

상태창에 매 턴 출력되는 값(정복률, 자금, 호감도)의 **증감 규칙은 상시 프롬프트 소유**입니다. 키워드북에 두면 트리거가 안 걸린 턴에 모델이 근거 없이 숫자를 지어냅니다. 출력은 매 턴인데 규칙은 가끔 로드되는 비대칭이 원인입니다.

상시 프롬프트로 올려야 하는 것: 산정 공식, **턴당 변화 상한**, 근거 없을 때의 기본값(예: `±0.03%p/턴, 근거 없으면 0`). 키워드북에는 그 값을 크게 움직이는 **특수 이벤트의 예외**만 남깁니다.

일반화하면 — **출력 빈도와 규칙의 로드 빈도가 어긋나면 환각이 납니다.** 매 턴 출력되는 것의 규칙은 매 턴 로드되어야 합니다.

---

## 8. 단축어는 같은 파일에 살지만 다른 문서가 소유한다

단축어는 키워드북과 **같은 물리 파일**(`build/keyword-book.md` 하단 `# Shortcuts` 섹션)에 담깁니다. 크랙의 5개 산출물 계약을 지키기 위한 배치일 뿐, 설계 규칙은 전혀 다릅니다 — 키워드북은 조건이 맞으면 자동으로 뜨고, 단축어는 플레이어가 눌러야 뜹니다.

**등록 규격·유형 분류·작성 원칙·표준 레시피·실사용 설계 규칙 10가지는 전부 [shortcuts-guide.md](shortcuts-guide.md)가 소유합니다.** 이 문서에는 사본을 두지 않습니다.

이 문서에서 알아야 할 것은 두 가지뿐입니다.

- **파일이 같다**: `crack_sync.py` 가 한 파일을 읽어 [키워드북] 탭과 [단축어] 탭에 나눠 등록합니다. 항목 블록과 `# Shortcuts` 섹션의 경계를 흐리지 마세요.
- **슬롯을 다투지 않는다**: 단축어는 3슬롯 예산과 무관합니다. 플레이어 호출 시점에만 주입되므로, 키워드북 슬롯이 부족하다고 해서 단축어로 옮기는 것은 해결이 아닙니다 — 자동으로 떠야 하는 것은 단축어가 될 수 없습니다.

---

## 9. SAFE vs UNSAFE 키워드북 분리 작성 원칙 (`keyword-book-safe.md` / `keyword-book-unsafe.md`)

크랙에서 **전연령/SAFE(안전판) 스토리**와 **19+ 성인/UNSAFE 스토리**를 배포할 때, 메인 시스템 프롬프트뿐만 아니라 **키워드북 또한 SAFE용과 UNSAFE용으로 분리하여 빌드**합니다.

| 구분 | **세이프 키워드북 (`keyword-book-safe.md`)** | **언세이프 키워드북 (`keyword-book-unsafe.md`)** |
|---|---|---|
| **발행 목적** | 전연령 / 청소년 이용가 스토리 배포 | 19+ 성인 / 고수위 롤플레이 스토리 배포 |
| **성애 19+ 모듈** | **완전 제외** (또는 풋풋한 뽀뽀/포옹 수준의 완곡 묘사) | **최상단(1~3번)에 필수 탑재** (`성애_19금`) |
| **A코드 체위 바인딩**| 없음 (일반 S01~S18 감정 표정만 연결) | A01~A15 성인 체위 씬 번호 및 노골적 감각 묘사 포함 |
| **공통 수록 항목** | 세계관 설정, 인물 명부, 전투/스킬 연출, 단축어 | SAFE의 모든 공통 항목 그대로 포함 |

### 🛠️ 자동 동기화 도구(`crack_sync.py`) 연동
* **SAFE 동기화 시**:
  ```bash
  python3 tools/sync/crack_sync.py sync <프로젝트명> --variant safe
  ```
  ➡️ `build/keyword-book-safe.md`를 우선 감지하여 크랙 스튜디오에 안전판 키워드북을 주입합니다.
* **UNSAFE 동기화 시**:
  ```bash
  python3 tools/sync/crack_sync.py sync <프로젝트명> --variant unsafe
  ```
  ➡️ `build/keyword-book-unsafe.md`를 우선 감지하여 `성애_19금`이 최상단에 배치된 성인 키워드북을 일괄 등록합니다.
*(※ 둘 중 하나만 존재하거나 기존 단일 `build/keyword-book.md`만 있는 경우에도 하위 호환성으로 자동 폴백 등록됩니다.)*
