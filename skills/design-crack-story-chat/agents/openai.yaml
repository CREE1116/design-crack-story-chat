interface:
  display_name: "크랙 스토리챗 설계"
  short_description: "2개 원본→최종 산출물 크랙 스토리챗 설계"
  default_prompt: "Use $design-crack-story-chat to author only story.md and characters.md, model important characters from initial mental states rather than reaction scripts, then compile the final artifacts: prologue, safe integrated prompt, unsafe integrated prompt, start prompt, and split or unified keyword-book sheets. Put system prompts only in the final build and use natural names instead of dotted namespaces."
