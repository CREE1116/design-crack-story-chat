# 변경 내역

## 0.10.18

- **MCP 서버 분리** — `mcp_server.py`, `http_mcp.py`, 관련 스크립트(`run-remote-mcp.sh`, `run-chatgpt-mcp.sh`) 및 예시 파일 제거. MCP 기능은 별도 저장소로 이전합니다.
- **에뮬레이터 개선**
  - `engine.turn(..., reply=)` 파라미터 추가 — CLI `--reply` 로 외부 API 없이 자가응답 검증 가능.
  - `webui.py`: 세션 variant를 turn 시점에 재사용, start_set 전환 시 restart 플래그 처리 개선.
  - `parser.py`: `build/start-sets/` 및 프로젝트 루트 `start-sets/` 양쪽 탐색 지원.
  - `crack_sync.py`: 임시저장(Draft Save) 버튼 자동 클릭, 키워드 태그 입력 React 호환 개선.
- **출력 계약 업데이트** — 대사 구분자 `이름 | "대사"` 형식 명시, 공문/서류/효과음 인라인 코드 규격 추가, 인위적 종결 질문 금지 명시.
- **파일 아키텍처**: `build/start-sets/` 를 허용 하위 디렉터리로 공식화.
- **문체 수정 지침 추가** (`references/prose-style-guide.md` §5) — 이미 돌아가는 작품의 문체를 고칠 때, 흩어진 서술 규칙을 찾아 고치는 대신 맨 뒤에 패치 블록을 덧붙입니다. 5요소(범위 선언·예시 지문·모방 금지선·강도 적응·우선순위)와 각각이 빠졌을 때의 실패 모드를 적었습니다. 예시만 주면 모델이 그것을 변주 대상으로 읽어 몇 턴 만에 같은 문형이 반복되기 때문에, 모방 금지선과 강도 적응이 예시와 함께 있어야 합니다.
- **레퍼런스 지도 신설** (`references/README.md`) — 사실 하나가 어느 문서 소유인지 28개 표, 자주 헷갈리는 경계 5개, 작업별 최소 로드 묶음 8개. 레퍼런스가 28개라 전부 읽으면 컨텍스트가 남지 않습니다.
- **중복 규칙 정리 — 사실 하나에 소유 문서 하나**
  - `prompt-writing.md` 의 6개 섹션(오프닝 앵커링·6중 잠금·부정편향·4단 말투·나레이터 페르소나·4대 연출 레시피)이 `crack-prompt-rules.md` 의 약한 사본이었습니다. 소유자 포인터 표로 교체하고 161줄 → 88줄. 원리(`production-patterns.md`)와 실제 프롬프트 문구(`crack-prompt-rules.md`)를 층으로 분리했습니다. 사본이 생기면 한쪽만 고쳐진 채 둘 다 살아남아 컴파일 때 어느 쪽이 맞는지 알 수 없게 됩니다.
  - `keyword-book.md` 팁 5의 압축 공식을 `semantic-compression.md` 포인터로 축약. 글자 수 숫자는 이제 한 곳만 고칩니다.
- **키워드북: 주입 위치와 상시 강화 항목**
  - `keyword-book.md` §3에 주입 위치를 명시했습니다. 키워드북은 유저의 가장 최근 메시지 바로 뒤(`<knowledge_base>`)에 들어가 출력에 가장 가깝습니다. 이 사실이 `crack-internal-runtime.md` 에만 있어 키워드북 문서에서는 배치가 **위치(키워드북이 강함) vs 확실성(통합 프롬프트가 매턴 보장)** 의 거래라는 것을 알 수 없었습니다.
  - 팁 8 신설 — 하단 상시 강화 항목. 문체처럼 매 턴 적용되지만 한 턴 빠져도 무너지지 않는 규칙을 최하단 상시 항목으로 두는 패턴. 성립 조건 4개(최하단 등록·최대 2개·본문 폐기 가능·HUD 라벨 아닌 트리거)와 `crack-emu` 의 대응 규칙(`always_on_trigger`, `high_match_entry_registered_high`)을 연결했습니다.
  - 팁 4의 "상시 트리거 절대 금기" 와 `validation.md` 의 "상시 항목은 하단에 최대 2개 허용" 이 충돌한 채 나란히 있었습니다. 사고로 상시 발동되는 경우(error)와 의도한 설계(조건부 허용)를 구분해 하나로 합쳤습니다.
  - 실사용 프로덕션 세션에서 나온 발견을 팁 2·4·7·9·10·11 로 반영했습니다. 대표적으로 — 출력 계약의 대사 구분자를 트리거로 재사용하면 언급과 발화가 정확히 갈립니다(대가는 1턴 지연). 키워드북은 인물을 등장시키지 못하고 등장 뒤에 살을 붙일 뿐이라, 등장 규칙은 상시 프롬프트 소유입니다. 3슬롯은 항목 개수가 아니라 동시 점등 수 문제라 조직도가 아니라 실제 co-occurrence 로 2인씩 묶고, 씬 인원 상한과 세트로만 작동합니다. HUD 에 매 턴 찍히는 수치의 산정 규칙을 키워드북에 두면 트리거 안 걸린 턴에 모델이 숫자를 지어냅니다.
  - 컴파일 후 `Info` 블록 × 전체 키워드 교차검사를 검수 항목으로 넣었습니다. 한 번의 검수에서 세 건이 걸렸고, 그중 둘은 래치되는 값(관계 단계 이모지, 획득 상태)이라 한 번 켜지면 세션 끝까지 슬롯을 점유합니다.
- **대사 구분자를 `*이름*| "대사"` 로 통일** — `output-contract.md`·`crack-prompt-rules.md`·`system-prompt-presets.md`·`opening-design-guide.md` 가 서로 다른 형식을 말하고 있었고, `crack-prompt-rules.md` 는 두 형식을 "또는" 으로 허용해 사실상 형식을 정하지 않고 있었습니다. 형식이 하나여야 화자 토큰을 키워드북 트리거로 재사용할 수 있습니다. 반각 `|`(U+007C)와 특수 채널의 전각 `｜`(U+FF5C)를 구분해 명시했습니다.
- **압축 Step 0 신설** (`semantic-compression.md`) — 압축의 1단계는 글자 줄이기가 아니라 중복 제거입니다. 상시 로드되는 계층에 이미 있는 정보는 압축 대상이 아니라 삭제 대상이며, 이 검사를 건너뛰면 중복을 열심히 압축한 뒤 "한도가 부족하다"는 잘못된 결론에 도달합니다.
- **글자 수 판정 기준을 UTF-16 code unit 으로 명시** — 이모지는 서로게이트 페어라 코드포인트로 세면 과소 측정되고, 통과했다고 판단한 산출물이 크랙에서 잘립니다. `check_prompt_length.py` 는 이미 `max(codepoints, utf16)` 로 판정하고 있었으나 문서가 "characters, not tokens" 로만 적혀 있어 오해를 낳았습니다.
- **단축어 문서를 키워드북에서 분리** — 같은 물리 파일(`build/keyword-book.md`)에 담기지만 설계 규칙은 전혀 다릅니다(자동 발동 vs 플레이어 호출). `keyword-book.md` §8 의 등록 규격·레시피 사본을 걷어내고 `shortcuts-guide.md` 단독 소유로 정리했습니다.
- **단축어 실사용 설계 규칙 10가지** (`shortcuts-guide.md` §7) — 실제로 널리 쓰이는 단축어(`요약`·`전개`·`사칭점검`·`스킵`·`OOC 오류보고`·`에타`)의 공통분모입니다. 첫 줄의 RP 중단 선언, 코드블록 형식 고정, 범위를 숫자로 자르기, 무인자 기본 동작 정의, 서사형의 PC 주권 종료 조항, 금지+대체 짝, "예시 外 창작 必", 자기점검형의 가시적 산출물, OOC형의 인간 흉내 금지, 인게임 UI 모사 시 문화 지정. 400자로 줄일 때 마지막까지 남길 셋도 함께 적었습니다.
- **플레이 가이드·추천 답변 지침 신설** (`references/play-guide.md`) — 지금까지 지침이 아예 없던 칸입니다. 모델이 아니라 **플레이어**가 읽는 자리이며, 상세설명(들어올까 고민하는 사람)과도 다릅니다(시작 버튼 앞에 선 사람). 4대 블록과 추천 답변 2대 용법, 추천 답변에 적은 이름·능력이 그대로 세션의 정사가 된다는 주의를 담았습니다.
- **한 줄 소개는 30자** — 문서마다 80자·100자로 갈려 있었고, 상세설명과 같은 것으로 오해되고 있었습니다. `story-description-guide.md` §0 에 세 필드(한 줄 소개 30자 / 상세설명 / 고정 댓글)의 구분표를 넣고 `crack_sync.py` 가 초과를 위반으로 보고하게 했습니다.
- **`crack_sync.py inspect` 를 인쇄기에서 검사기로** — 한도를 출력만 하고 항상 "완벽하게 부합합니다" 를 찍고 있었습니다. 실제로 세어 위반 시 exit 1 을 냅니다(UTF-16 기준). 붙이자마자 hunter 예제의 한 줄 소개 80자 초과를 잡았습니다.
- **[시작 설정] 탭 주입을 위치 추정에서 placeholder 기반으로 교체** — 보이는 `textarea` 순서로 잡던 코드가 한 칸씩 밀려 **프롤로그가 시작 상황 칸에, 시작 프롬프트가 플레이 가이드 칸에** 들어가고 있었습니다. 이제 각 칸의 placeholder 문구로 찾고, 못 찾으면 **아무 데도 쓰지 않고 경고만** 냅니다. 다중 시작 세트에서 '설정 추가' 버튼을 못 찾으면 앞 세트를 덮어쓰지 않도록 중단합니다. 플레이 가이드와 추천 답변(최대 3) 주입을 함께 추가했습니다.
- **검사기 3건 수정 — 전부 스킬이 규정한 형식을 못 읽던 것들**
  - `check_project_layout.py` 가 `start-sets/` 를 "정체 불명 중간 산출물" 로 거부했습니다. `references/start-sets.md` 가 정본으로 규정한 위치인데 허용 목록에 없었습니다.
  - `check_image_assets.py` 가 인물 명부를 폐기된 `char.` 점 표기로만 파싱해, SKILL.md 이름 규칙이 정한 `인물-이름` 하이픈 라벨을 쓰는 프로젝트에서 **명부 전체를 못 읽고 인물 16명을 전부 "정체 불명"으로** 보고했습니다. 게다가 같은 파일에 명부 파서가 두 벌 있었고(`parse_roster()` 와 main 안의 사본) 실제 판정에 쓰이는 것은 사본이었습니다. 둘 다 고쳤습니다.
  - `check_build.py` 의 파생물 허용 목록에 `play-guide.md`·`recommended-replies.md` 를 추가했습니다.
- **단축어 경제성 규칙** (`shortcuts-guide.md` §2.5) — 단축어 호출 한 번은 응답 한 번이고 플레이어에게는 크래커 한 번분입니다. 출력이 세 줄이든 400자 가득이든 값이 같으므로 **짧게 끝날 단축어는 만들지 않습니다**. 특히 `상태창` 단축어는 순수 낭비입니다 — HUD 는 출력 계약에 따라 이미 매 응답 하단에 붙으므로, 방금 화면에 있던 것을 돈 내고 다시 받는 셈입니다. 판별 질문은 "이 단축어가 없으면 플레이어가 무엇을 못 하나" 이고, 답이 "이미 화면에 있는 걸 다시 본다" 면 만들지 않습니다.
- **플레이 가이드는 서식이 아니라 역할을 가르칩니다** — 초안이 널리 쓰이는 어느 가이드의 표기(괄호·이모지·구분선 조합)를 그대로 옮겨 놓고 있었습니다. 그 조합은 **그 제작자의 서명**이라 읽는 사람이 먼저 알아봅니다. 4대 블록은 역할로만 남기고 표기는 작품 세계에서 가져오도록 고쳤습니다(회사물이면 사내 공지, 사극이면 방문). 추천 답변의 유저노트 예시도 문구째 싣지 않고 무엇을 보여줘야 하는지로 바꿨습니다.
- **임시저장이 성공을 자칭하고 있었습니다** — `save_crack_draft()` 는 독스트링에 "verify toast/save state" 라고 써놓고 실제로는 클릭만 하고 성공을 출력했습니다. 눌러도 크랙이 저장을 거부하는 경우가 있어, 로그만 믿으면 저장되지 않은 작업을 저장됐다고 보고하게 됩니다. 이제 저장 표시를 찾은 뒤에만 성공이라고 말하고, 못 찾으면 미확인이라고 말한 뒤 화면을 덤프합니다.
- **실측 제약을 한 표로** (`crack-auto-sync-guide.md` §5.5) — 한 줄 소개 30자, 플레이 가이드 500자, 시작 세트 3개, 추천 답변 3개. 추측한 값은 적지 않았고, 세 가지는 전부 실패 시 DOM 덤프로 밝혀졌습니다.
- **플레이 가이드 상한은 500자** — 초과분은 경고 없이 잘려 마지막 블록이 통째로 사라집니다(실측: 637자를 넣었더니 화면에 500자만 남음). `inspect` 가 위반으로 보고합니다. 500자는 4대 블록을 다 넣기에 빠듯해서 **무엇을 뺄지가 설계의 대부분**이며, 줄일 때는 구분선을 남기고 문장을 자릅니다 — 훑는 사람에게는 구조가 내용보다 먼저 보입니다.
- **`meta.md`의 `- enabled: false`** — 상한을 넘겨 기획한 시작 세트를 지우지 않고 보류합니다. 폴더를 지우면 그 루트를 다시 쓸 때 처음부터 쓰게 됩니다.
- **갱신 실행에서는 기존 세트 탭을 눌러 들어갑니다** — 세트가 이미 있는 작품을 다시 싱크할 때 [설정 추가] 를 누르려 하면 상한에 막혀 2번째 세트부터 주입이 멈췄습니다. 이제 제목이 같은 탭을 먼저 찾아 클릭하고, 없을 때만 추가를 시도합니다.
- **크랙 시작 세트 상한은 3개** (`start-sets.md` §1.5) — 3개가 차면 [설정 추가] 버튼 자체가 사라집니다(실측). 4번째 폴더는 주입되지 않으므로 어느 셋을 내보낼지 먼저 정합니다. `inspect` 가 초과를 위반으로 보고하고, 동기화는 상한에 걸리면 남은 세트를 주입하지 않고 중단합니다(버튼 없이 계속하면 앞 세트를 덮어씀).
- **프롤로그 칸은 placeholder 가 없다** — 화면의 "자동 생성 기능을 활용하면…" 문구는 옆 [자동 생성] 버튼의 도움말이지 칸의 속성이 아니었습니다. 그래서 이 칸만 문구로 찾을 수 없어 조용히 비어 있었습니다. 시작 상황 칸(placeholder 가 확실한 칸)을 앵커로 삼아 **그 직전의 placeholder 없는 textarea** 를 고르도록 바꿨습니다 — 순서를 세는 방식과 달리 칸이 늘어도 함께 움직입니다.
- **`crack_sync.py` 진단 — 못 찾으면 실물을 찍는다**
  - 칸이나 버튼 탐색에 실패하면 그 자리에서 현재 화면의 입력 칸·버튼을 전부 덤프합니다(태그, `placeholder`, `data-placeholder`, `aria-placeholder`, `aria-label`, 현재 글자 수). placeholder 를 추측해 셀렉터를 고치는 왕복을 없앱니다.
  - placeholder 셀렉터를 `textarea` 한 종류에서 `input`·`contenteditable[data-placeholder]`·`aria-placeholder` 까지 넓혔습니다. 크랙의 칸이 전부 `textarea[placeholder]` 인 것은 아니고, 한 종류만 보면 그 칸은 조용히 빈 채로 저장됩니다.
  - 신규 생성 모드에서 **만들어진 작품의 에디터 주소를 출력**합니다. 이게 없으면 다음에 같은 작품을 갱신할 방법이 없어 실행할 때마다 새 작품이 하나씩 늘어납니다.
- **`crack_sync.py --title-suffix`** — UNSAFE 판을 같은 계정에 별도 작품으로 임시저장할 때 제목을 구분합니다(`--title-suffix ' U'`). 핫리로드 재적재 경로가 여러 개라 로더 한 곳에서 붙입니다.
- **예제 작품 갱신** (`examples/hunter`) — 새 대사 구분자 적용(UNSAFE 는 재압축해 6,991자), 프롤로그를 648→900자로 증량(타겟 밴드 미달이었음), 문체 항목을 1글자 트리거(`.`·`다`·`요`)에서 종결 어미(`했다`·`였다`·`진다`)로 바꾸고 본문을 문체 패치 블록 5요소로 재작성, `- Logline:` 24자 신설.

## 0.10.17

- **크랙 에뮬레이터 / QA 하네스 동봉** (`tools/crack-emu/`).
  - 빌드를 파싱해 크랙 관측 순서대로 프롬프트를 조립하고, LLM에 보내고, 응답을 **그 빌드 자신의 출력 계약**에 대조합니다. 사람용 웹 UI(`serve`)와 에이전트용 JSON CLI가 같은 엔진 위에 있습니다.
  - **프로젝트마다 계약이 다릅니다.** 대사 구분자(`|` / `:`), 지문 표기, 상태창 펜스(라벨 유무 포함), 이미지 체계(번호 / 슬러그), 금지 조항을 프롬프트에서 유도하고, **근거가 없으면 해당 규칙을 끕니다.** 채택한 적 없는 관례를 어겼다고 보고하는 검사기는 진짜 결함을 소음에 묻기 때문입니다. `qa-contract.yaml` 로 덮어쓸 수 있습니다.
  - **3슬롯을 실측합니다.** 가상 장면 목록 시뮬레이션 대신 실제 턴을 돌려 발동 로그(JSONL)를 쌓고, `report` 가 슬롯 부족으로 **드롭된 항목**, 한 번도 안 걸린 항목, 매 턴 걸리는 항목을 냅니다.
  - `lint` 가 정적으로 잡는 것: 400자·키워드 5개 한도, 중복, **부분문자열 오발동**(`라임`이 `슬라임`에 걸리는 종류, 조사 결합은 정상으로 간주), 우선 모듈이 3슬롯 밖에 등록됨, 상태창 고정 항목명을 트리거로 씀, 성인 A코드가 메인 프롬프트로 누출, 단축어 이름 앞 `/`, **프롤로그 이미지의 인물번호·이름 불일치**.
  - 프로바이더는 전부 OpenAI 호환. `ollama`(네이티브 `/api/chat` 으로 `num_ctx` 지정), `openrouter`, `gemini`, `openai`, 그리고 모델 없이 배관만 검증하는 `echo`.
- **다중 시작 프롬프트를 시작 세트로 규격화** (`references/start-sets.md`).
  - `start-sets/<id>/{meta.md,prologue.md,start-prompt.md}` 가 정본이고 `build/` 의 두 파일은 생성물입니다. 프롤로그와 첫 상황은 **한 쌍으로만** 선택됩니다. 짝이 어긋나면 첫 턴이 무너지기 때문입니다.
  - `crack-emu sets` 로 목록·반영·이관. 폴더를 만들면 자동 인식되며 어디에도 등록하지 않습니다.
  - `crack_sync.py` 에서 부서 폴더명·제목 하드코딩을 제거하고 `meta.md` 를 읽도록 일반화했습니다.
- **키워드북 검증기를 스킬에서 하네스로 이관** (`check_keyword_book.py`, `check_kb_slots.py` 삭제).
  - 동일한 항목 한도를 검사하면서, 정적 검사로는 볼 수 없는 실제 슬롯 거동까지 측정합니다. `validate.sh` 와 `check_build.py` 가 하네스를 호출하도록 배선했습니다.

## 0.10.16

- **크랙 내부 런타임 아키텍처 및 프롬프트 주입 파이프라인 전면 규명** (`references/crack-internal-runtime.md`).
  - **3단계 런타임 파이프라인 분석**: `[System Message]`(최상단) ➡️ `[Chat History]`(중간) ➡️ `[User Message] + ## Additional Information & Rules`(최하단).
  - **하단 주입(`<knowledge_base>`)의 Recency Bias 입증**: 왜 키워드북이 메인 프롬프트보다 강력하게 모델을 지배하는지 런타임 위치 원리 규명.
  - **플랫폼 내장 요소 충돌 방지**: 크랙 자체의 소아성애 거부 가드, 스탯 레벨 정의(`[Story State]`), 이미지 키워드 출력 폼(`{{img::keyword}}`), 비공개 개인 세션 탈옥 구문, 생각 토큰 0 토큰 제한 등의 내장 주입 구조 정리.
  - `SKILL.md`, `references/crack-prompt-rules.md`, `references/keyword-book.md`에 공식 링크 및 최적화 전략 연계.

## 0.10.15

- **단축어 이름(Name) 앞 슬래시(`/`) 전면 배제 및 중복 입력 방지** (`references/shortcuts-guide.md`, `tools/sync/crack_sync.py`).
  - 크랙 웹/모바일 UI는 단축어 버튼 클릭 시 자동으로 `/` 접두어를 붙여주므로, 이름에 `/`가 들어가면 `//명령어`처럼 슬래시가 2번 입력되는 결함 방지.
  - `crack_sync.py` 파서에 단축어 이름 앞 `/` 자동 정제(`lstrip("/")`) 방어 로직 추가.
  - 모든 레시피 예시의 `name:`을 순수 텍스트(`상태창`, `커뮤니티`, `속마음`, `요약` 등)로 표준화.
- **이미지 프롬프트 표준 JSON 배열 출력 규격 정립** (`references/novelai-prompt-engineering.md`, `tools/images/compose_character.py`).
  - NovelAI 일괄 생성기 연동용 표준 포맷: `[{"name": "...", "prompt": "...", "uc": "..."}]`.
  - `compose_character.py`에 `--output-json` 옵션 탑재.
- **스토리 상세설명(Story Description) 6대 황금 블록 바이블 개편** (`references/story-description-guide.md`).
  - 클릭형 대표 배너 이미지 링크, 비주얼 볼륨 증명(에셋 통계표), 3초 후킹 시놉시스 & 현실적 딜레마, 핵심 주연 라인업 1줄 요약, 플레이어 가이드 & 단축어 팁, 제작자 코멘트 소통창구.
  - 마크다운 시각 연출 노하우(가로선 구분, 벽돌 텍스트 금지, 이모지 앵커링) 상세 수록.

## 0.10.14

- **단축어(Shortcuts) 설계 가이드 신설 및 3대 분류 체계 공식화** (`references/shortcuts-guide.md`).
  - 메타형 (코드블럭 필수, RP 중단 명시, 캐릭터 비인지): `/상태창`, `/커뮤니티`, `/속마음`, `/요약`.
  - 서사형 (코드블럭 생략, 소설 서술 전개, 캐릭터 인지): `/돌발위기`, `/다음날로`, `/밀착`, `/합방`.
  - 기능형 (사칭 교정, 문체 전환, 캐릭터 비인지): `/사칭교정`, `/하드보일드`, `/라노벨`.
  - 크랙 단축어 플랫폼 한도(이름 ≤10자, 설명 ≤30자, 프롬프트 ≤400자) 엄수 및 5단계 제작 프로세스 수립.
- **크랙 플레이라이트 자동 동기화 도구 전면 고도화** (`tools/sync/crack_sync.py`, `references/crack-auto-sync-guide.md`).
  - 신규 스토리 생성(URL 생략 시 자동 스튜디오 ➡️ 스토리 에디터 진입) vs 기존 프로젝트 로드(`--url`) 모드 분리.
  - 한 줄 소개(80자 단문 로그라인)와 상세설명(`story-description.md` 전체 마크다운) 분리 주입.
  - 키워드북/단축어 재입력 시 기존 항목 자동 감지 및 전수 삭제(`clear_existing_keywords`) 후 1:1 재등록 (중복 누적 방지).
  - 단축어 프롬프트 멀티라인 불릿 및 코드블럭 파싱 지원.
- **키워드북 성인 모듈 최상단(1순위) 배치 및 상황코드 가변성 확립** (`references/keyword-book.md`, `references/image-output-rules.md`).
  - 성인 상황코드의 장르별 가변형 템플릿 원칙 확립.
  - 3슬롯 중단 드랍 방지를 위한 성애/특수 연출 모듈 최상단(1~3번) 배치 강제.
  - 1턴 씨앗(메인) + 2인 1항목 200자 페어링 압축 기법 공식화.
- **문체 및 감각 서술 레시피 가이드 신설** (`references/prose-style-guide.md`).
  - 감정의 신체화, 오감 환경 투영, 감정의 대리 장치, 불확실성 독백, 니시오 이신/다자이 오사무 스타일.
- **작품 상세설명(`story-description.md`)과 고정 댓글용 명부(`summary-comment.md`) 분리**.
- **구버전 점 표기(`kb.directive_nsfw` 등) 전면 일괄 제거**.

## 0.10.13

- **심리 생성 기반 캐릭터 설계 방법론 탑재** (`references/character-generation.md`).
  - 세계의 진실 / 초기 정신 상태 / 관찰 행동 분리.
  - Temperament·Belief·Desire·Self·Memory·Relationship·State·Trait 슬롯과 심리 생성 순서 표준화.
  - Unknown 유지, Reflection 게이팅, T0 초기조건 설계, State/Trait 분리 추가.
- **점식 캐릭터·원본 네임스페이스 재발 방지**.
  - 템플릿의 점식 예시 제거.
  - `check_naming.py` 추가 및 통합 검증 연결.
  - 구원순애 프리셋에 심리 생성 엔진 블록 추가.

## 0.10.12

- **실전 구원순애 통짜프롬 핵심 노하우 전면 흡수 및 공식 지침 표준화** (`references/output-contract.md`, `references/character-personality.md`, `references/status-window-guide.md`, `references/prompt-writing.md`).
  - **출력 서식 정밀화 (`output-contract.md`)**:
    - 대사 내 괄호 지문 삽입 금지 (`(고개를 끄덕이며) 응` ❌ ➡️ 지문/대사 줄 분리 ⭕).
    - 정확한 신체 수치 나열 금지 (`171cm의 눈높이` ❌ ➡️ 문학적 비유 ⭕).
    - 양피지/깃펜/글씨체 떨림/전서구 서신 채널 (`>**이름(✉)**\n"내용"`) 및 속마음 문법 (`*──text*`) 추가.
    - 날씨의 시청각·감각적 서술 연계 (창문 때리는 빗소리, 비바람에 젖은 머리카락, 천둥소리에 깃펜 놓침).
  - **캐릭터 심층 설계 (`character-personality.md`)**:
    - 인물 결핍과 역린(발작버튼) & PTSD/트라우마 회복 곡선 (`🥀피폐 ➡️ 🌿안정 ➡️ 🌸회복 ➡️ ⚔️결연`).
    - 나이별 결정적 과거사 앵커링 (예: 7세 첫 만남, 15세 각성, 20세 패퇴).
    - 상태창 복장(단어 나열형) 및 몸상태(호전 가능한 부상/통증) 연동 규격.
  - **텍스트 품질 4대 규약 (`prompt-writing.md`)**:
    - 문단 정보 밀도(최소 3~4문단 고유 정보), 시대 고증(번역체·신조어 금지), 지각 범위 잠금(시야 밖 투시 금지), 문학적 종결어미('였다, 뿐이다, 했다').

## 0.10.11

- **중세 판타지 / 구원 순애 / 피폐 로맨스 100% 순수 시스템 프롬프트 프리셋(Preset 5) 추가** (`references/system-prompt-presets.md`).
  - 구원순애 통짜프롬 구조를 표준 8대 블록으로 리팩토링한 100% 순수 뼈대 탑재.
  - 명암·온도 대비(가혹한 현실 ↔ 따뜻한 안식처), 외생적 이벤트, 경제/물가 1줄 앵커링, 편지(✉) 채널, 비피학적 현실 저항 가드, 공식 25종 관계 이모지 연동 완비.

## 0.10.10

- **영문 슬러그 잔재(`char.xxx`) 템플릿 및 레퍼런스 전수 제거 완료** (`assets/story-chat-template/characters.md`, `references/file-architecture.md`, `references/story-model.md`, `docs/usage.md`).
  - 템플릿 및 명세서 내 모든 `char.example`, `char.other`, `char.ban-hosik` 예시를 `인물 이름 「이명/직책」` 등 자연스러운 한국어 인물명으로 전면 교체.

## 0.10.9

- **실전 4대 서사 연출 레시피 및 오프닝 안전 수칙 표준화** (`references/opening-design-guide.md`, `references/production-patterns.md`, `references/prompt-writing.md`).
  - **`{{user}}` 조사 결속 깨짐 방지**: `{{user}}는`, `{{user}}을` 직결 금지 및 3인칭 대명사/역할 호칭 처리 원칙 확립 (`신창섭는` 찐빠 원천 차단).
  - **초기 환경 변수 100% 구체적 앵커링**: 템플릿 변수 누출 방지 및 날씨/시간/턴수 픽스.
  - **초반 위기 앵커링**: 당면한 부상/시간 제약/위기를 통한 1턴 행동 동기 부여.
  - **4대 실전 서사 연출 레시피 탑재**:
    1. 명암 & 온도 대비 연출 (Atmospheric Contrast: 가혹한 현실 ↔ 따뜻한 안식처)
    2. 외생적 이벤트 트리거 (Exogenous Event Injection: 루프/정체 시 외부 사건 자율 주입)
    3. 경제 & 물가 기준 1줄 앵커링 (Economic Scale Anchor: 화폐 가치 인플레이션 방지)
    4. 비피학적 현실 저항 가드 (Realistic Emotional Resistance: 가학 행위 시 후희 냉소 및 결별 가능)

## 0.10.8

- **공식 25종 관계 이모지 매트릭스(Relationship Emoji Matrix) 전면 통합** (`references/status-window-guide.md`, `references/character-personality.md`, `references/crack-prompt-rules.md`).
  - **25단계 공식 관계 분류 체계 확립**:
    - 위험/사망: `💀` (사망+등장불가)
    - 적대/갈등: `🤬` (혐오), `😠` (갈등), `☹️` (짜증·실망), `🤨` (경계·의심)
    - 사무/중립: `😐` (일면식), `💼` (업무관계), `😑` (중립·무관심)
    - 호감/우정: `🙂` (가벼운호감), `😀` (뚜렷한호감), `😄` (강한호감·호의), `🤝` (친구), `🫂` (절친)
    - 로맨스 태동: `😳` (이성으로인식), `☺️` (이성적호감), `🥰` (호감심화), `❣️` (짝사랑), `🫠` (집착·불안)
    - 결합/성애/특수: `💓` (연인), `💝` (연인된 후 ㅅ스), `❤️🔥` (ㅅ스파트너), `🖤` (혐관), `💔` (결별), `💍` (결혼), `💞` (하렘)
  - 상태창 `[관계]` 슬롯 및 인물별 어조/호칭/거리감 동적 진화(SlowBurn) 1:1 표준 연동.

## 0.10.7

- **크랙 동기화 툴 브라우저 영구 상주 및 대화형 핫리로드(Hot-Reload) 루프 구축** (`tools/sync/crack_sync.py`, `references/crack-auto-sync-guide.md`).
  - **입력 후 브라우저 닫힘 방지**: 자동 주입 완료 후 창을 닫지 않고 작가가 직접 임시저장/발행할 수 있도록 브라우저 세션 영구 유지.
  - **대화형 핫리로드 (`r`)**: 원본 파일 수정 후 `r` 키만 누르면 브라우저를 닫지 않고 변경된 프롬프트와 19개 키워드북 즉시 덮어쓰기 재주입.
  - **실시간 버전 스위칭 (`v`)**: SAFE ↔ UNSAFE 시스템 프롬프트 실시간 전환 지원.

## 0.10.6

- **크랙(Crack) Playwright 브라우저 자동 동기화 툴 탑재** (`tools/sync/crack_sync.py`, `references/crack-auto-sync-guide.md`).
  - **약관 위반 없는 안전한 브라우저 세션 자동화**: 실제 유저 로그인 세션을 1회 캡처(`~/.crack/auth_state.json`)하여 계정 제재 및 WAF 봇 차단 위험 원천 차단.
  - **전 산출물 원클릭 1:1 자동 주입**: 프롤로그(1,000자), 시작 프롬프트(1,000자), 시스템 프롬프트(7,000자), 키워드북 19개 항목(제목+키워드+본문), 단축어(이름+설명+프롬프트), 상세 설명란 자동 매핑.
  - **3대 서브커맨드 완비**: 1회 세션 캡처(`auth`), 필드 매핑 및 글자수 사전 검사(`inspect`), 에디터 실제 주입(`sync`).

## 0.10.5

- **NovelAI 이미지 스킬 엔진 전면 흡수 및 정밀 프롬프트 엔지니어링 가이드 구축** (`references/novelai-prompt-engineering.md`, `SKILL.md`).
  - **정밀 1인칭 POV 카메라 & 시선 기하학 탑재**: 시점 소유자, 앵글(`from above/below/side`), 시선 타겟팅(`looking at viewer`, `eye contact`, `looking down`, `closed eyes`), POV 증거(`pov hands`, `female pov`, `taker pov`).
  - **일반 상황코드(S01~S18) 공식 Danbooru 태그 사전**: 18종 감정별 검증된 단부루 태그 세트 수록.
  - **성인 상황코드(A01~A15) 정밀 체위 기하학 사전**: 15종 성애 체위별 공식 단부루 체위 태그 및 결속 기하학 수록.
  - **3단 레이어 분리 컴파일 아키텍처**: Base Character (불변) + Pose/Emotion (가변) + Scene/Lighting (환경) 결합 공식 정립.

## 0.10.4

- **NovelAI V5 레퍼런스-프리(Reference-Free) 캐릭터 100% 고정 6대 실전 비법 전면 통합** (`references/character-appearance-guide.md`).
  - **두상 8할 고정 원칙**: 헤어 색상 명도(`light`/`dark`) 접두사, 형태+결 결합, 앞머리(`bangs`) 필수 지정, 눈매+동공 결속, `1.5::...::` 두상 가중치 묶음 도입.
  - **체형 앵커 필수 지정**: 아티스트 스타일에 따른 체형 흔들림 방지용 바스트 등급(`small/medium/large breasts`, `flat chest`) 필수 고정.
  - **의상 4단 자연어 덩어리**: `[색상] + [소재/질감] + [아이템] + [디테일/장식]` 자연어 결합구 체계화.
  - **구도별 스마트 하의 분기**: 상반신(`upper body`) 구도 시 하의 생략 규칙.
  - **표정 전환 시 눈/입 가중치 오버라이드**: 베이스 눈매와 충돌하는 눈/입 표정 태그에 `1.5~1.8::...::` 가중치 억제 기법 명문화.

## 0.10.3

- **캐릭터 원본 명세(`characters.md`) 내 구형 슬러그 문법(`char.xxx`) 완전 제거 및 자연어 단일화**.
  - `## \`char.shim-gaeul\` — 심가을` 같은 프로그래머식 난해한 문법을 걷어내고 `## 심가을 「測候」` (또는 `## 에리카 아르덴 01`)와 같은 자연스러운 한국어 인물명 헤딩으로 단일화.
  - `check_image_assets.py` 린터에서 한국어 인물명과 이미지 슬러그를 양방향 자동 매핑하도록 고도화.

## 0.10.2

- **키워드북 초경량 직관형 포맷 전면 개편 및 린터 동기화** (`references/keyword-book.md`, `scripts/check_keyword_book.py`, `scripts/check_kb_slots.py`, `examples/hunter/build/keyword-book.md`).
  - 복잡한 영어 ID(`kb.xxx`)와 시스템 메타 태그(`activation_setting`, `## Entry text`)를 걷어내고, 크랙 UI 3대 필수 요소(항목명, 키워드, 내용)로 초경량화.
  - `- 키워드: a, b, c` 및 `- 내용: ...` 직관적 문법 지원 (구형 포맷 100% 하위 호환 유지).
  - `check_keyword_book.py` 및 `check_kb_slots.py` 검증기에서 한국어 제목 기반 로드 및 한도 검사 완벽 지원.

## 0.10.1

- **시스템 프롬프트용 초압축 이미지 출력 규칙 템플릿 정립** (`references/image-output-rules.md`, `references/system-prompt-presets.md`, `references/output-contract.md`).
  - 글로벌 상황번호 매트릭스(`S01~S18`, `A01~A15`) 기반 130자 내외 초압축 템플릿 구축.
  - 화자 첫 등장 시 1장, 감정 변화 시 1장, 동일 감정 연속 발화 시 재출력 생략 등 런타임 제어 원칙 명문화.
- **키워드북 19+ 성애 연출 모듈(`kb.directive_nsfw`) 5대 설계 기둥 및 표준 템플릿 분리** (`references/keyword-book.md`, `references/image-output-rules.md`).
  - 플레이어 주도권/동의, 신체·감각적 정밀 묘사, 오프닝 앵커링 분량 제어, 절정과 사후 여운(필로우토크), A코드 성인 이미지 동적 호출 5대 기둥 정립.
  - 긴장감/거리감 절제형 문체 및 감각 집중형 문체 변주 가이드 제공.
- **부정편향 완충망 & 뻔함 방지 엔진(Anti-Cliché Engine) 명문화** (`references/production-patterns.md`).
  - 맹목적 사이다물, 평면적 악역, 인스턴트 호감, 기계적 번호선택지 강제, 고정 포맷 반복 등 5대 클리셰 차단 6중 그물망 체계화.

## 0.10.0

NovelAI 및 단부루 2024 공식 위키 DB 기반의 **초정밀 비주얼 엔지니어링 시스템**, **배경 2대 갈래 컴파일러**, **스토리 상세설명란 7대 완성형 배포 명세서**, 및 **신규 빌드 산출물(`character-design.md`, `scene-design.md`, `summary-comment.md`)**을 전면 구축했다.

### 추가
- **단부루 2024 공식 위키 DB 검색 도구 탑재** (`tools/images/search_tag.py`): 로컬 SQLite DB(`wiki.sqlite3`) 연동하여 태그명, 동의어, 정의 본문 실시간 검색 지원.
- **캐릭터 비주얼 지문 컴파일러 및 린터 구축** (`tools/images/compose_character.py`):
  - **체형 4대 지표**: 가슴 볼륨, 근육/복근, 골격 실루엣, 신체 디테일(허벅지 갭 등).
  - **헤어 3요소 결속**: `[색상] + [형태] + [기장]`.
  - **안면/두상 7대 파츠**: 얼굴형, 피부톤, 귀 형태/피어싱, 눈매, 동공, 눈썹/속눈썹, 코/입, 표식.
  - **의상 전 파츠 색상 결속**: 상의, 하의, 아우터, 장갑, 양말, 신발 전 파츠 색상 형용사 린팅.
  - **스마트 2단계 UC 도출**: 기본 해부학 방어 + 설정 반대 왜곡 자동 배제.
  - 신규 공식 빌드 산출물 `build/assets/character-design.md` 자동 생성 지원.
- **배경 2대 갈래 설계 규격 및 컴파일러 탑재** (`references/scene-design-guide.md`, `tools/images/compose_character.py`):
  - **🖼️ 풍경화형 단독 배경 (`Pure Scenery`)**: `no humans` 락, 광각 파노라마, 건축미, 대기 원근감.
  - **👥 인물 배치형 결속 배경 (`In-Scene`)**: 중경 지지대/가구, 피사계 심도(아웃포커싱), 인물 지향 조명.
  - 신규 공식 빌드 산출물 `build/assets/scene-design.md` 자동 생성 지원.
- **스토리 상세설명란 7대 완성형 배포 명세서 확립** (`references/story-description-guide.md`, `build/assets/summary-comment.md`):
  - 1. 클릭형 배너 링크 (`[![작품명](.../banner.webp)](.../)`)
  - 2. 이미지 에셋 통계 정리표 (`| 등장인물 | 🖼️ | 🔞 |`)
  - 3. 제작자 코멘트 (로그라인, 핵심 개념, 자유 안내문)
  - 4. 등장인물 상세 소개 (학년/진영별 번호 · 소속 · 나이 · MBTI · 1줄 역할)
  - 5. 인게임 이미지 규칙 & 상황코드 (S01~S18 일반 감정, A01~A15 성인 체위)
  - 6. 연간 일정 및 타임라인 상세
  - 7. 가문/세력 상세 설정 및 플레이어(당신)의 위치.
- **특수 채널 기호 선택적 적용 및 미사용 시 완전 생략 원칙 명문화** (`output-contract.md`, `crack-prompt-rules.md`):
  - `💬`(통신), `👾`(기계음) 등은 해당 세계관에 존재하는 경우에만 사용하며, 무협·사극·일상물 등에서는 프롬프트와 범례에서 완전히 제외.

### 변경
- **웹 쇼케이스 템플릿 정제** (`tools/images/web_template/`): 주소 복사 UI 및 19+ NSFW 모드 토글을 제거하고, 순수 공식 캐릭터 도감 및 에셋 뷰어로 경량화.
- **빌드 검증기 파생 산출물 허용 목록 업데이트** (`scripts/check_build.py`): `character-design.md`, `scene-design.md`를 공식 허용 파생물로 등록.
- **예시 프로젝트 및 전 문서 고유명사 정제**: 특정 상업 작품명의 잔여 흔적을 완전히 제거하고 순수 표준 레퍼런스화.

## 0.9.10

- **이미지 생성 프롬프트 잔여 서술 완전 제거 및 스킬 순수화** (`tools/images/README.md`, `SKILL.md`).
  - 생성 모델(SDXL, Pony, Illustrious, Flux) 및 프롬프트 문법 관련 잔여 테이블 완전 삭제.
  - WebP 일괄 변환, Cloudflare Pages 호스팅, 인터랙티브 웹 쇼케이스 템플릿 중심으로 툴링 문서 정제.

## 0.9.9

- **캐릭터별 상태(`💋`) vs 키워드 감지 시스템 전역 알럿(`🔞`) 역할 분리 확립** (`status-window-guide.md`, `crack-prompt-rules.md`).
  - `💋`: `[관계]` 슬롯 내부에서 특정 캐릭터에 대한 스킨십·키스·로맨틱 상호작용 진행 상태 표시.
  - `🔰` ↔ `🔞`: 상태창 맨 끝 `[상황]: "..." ｜ [모드]`에서 키워드북 엔진을 트리거하는 단일 전역 시스템 알럿으로 명문화.

## 0.9.8

- **18금(🔞) 이모지 선제 표출 규칙 및 키워드북 연동 메커니즘 명문화** (`status-window-guide.md`, `crack-prompt-rules.md`, `system-prompt-presets.md`).
  - 키스, 스킨십 심화, 침실 이동 등 성적(19+) 장면의 전조 감지 시 이번 턴 상태창 `[상태]` 슬롯에 반드시 `[🔞]` 또는 `[💋]`을 선제 표출하도록 규정.
  - 상태창의 `🔞` 이모지를 통해 시스템 키워드 엔진이 다음 턴에 성애 연출 모듈(`kb.directive_nsfw`)과 `s` 시리즈 성인 CG 규칙을 지연 없이 즉각 로드하도록 연동.
- **공식 쇼케이스 웹페이지 GitHub Pages 배포 완료** (`https://cree1116.github.io/design-crack-story-chat/`).
  - SVG 다크 모노그램 플레이스홀더를 채운 완성형 웹 쇼케이스 갤러리를 `gh-pages` 브랜치로 자동 배포.

## 0.9.7

- **쇼케이스 웹 템플릿 실시간 프리뷰 및 SVG 플레이스홀더 렌더러 탑재** (`tools/images/web_template/app.js`).
  - 실제 이미지가 채워지기 전에도 완성도 높은 다크 판타지 모노그램 카드를 보여주는 인라인 SVG 자동 생성기 탑재.
  - 마크다운 복사 UI를 자연스러운 **'이미지 주소 복사'** 및 **호출 경로 힌트**로 직관화.

## 0.9.6

- **인터랙티브 웹 쇼케이스 템플릿 대규모 기능성 및 비주얼 업그레이드** (`tools/images/web_template/`).
  - **캐릭터 인스펙터 모달**: 카드 클릭 시 고해상도 초상화, 프로필, 표정 바리에이션 선택기(`a01`, `a02`... 및 `s01`...), 원클릭 마크다운 태그 복사 기능 탑재.
  - **4대 카테고리 탭 탐색**: 인물 도감(Characters), 배경·장소(Scenes), 위협·몬스터(Mobs), 이벤트 CG(Events) 탭 전환 지원.
  - **19+ NSFW 모드 토글**: 성인 씬/CG를 안전하게 블러 및 필터링할 수 있는 로컬 저장소 연동 모드 스위처 탑재.
  - **실시간 검색 & 다중 필터 & 정렬**: 인물명/역할/키워드 실시간 검색, 그룹 필터 알약, 번호순/이름순 정렬 지원.
  - **글래스모피즘 & 애니메이션 폴리싱**: 토스트 알림, 플로팅 맨 위로 가기 버튼, 카드 호버 시 대사 팝업 및 네온 글로우 효과 반영.

## 0.9.5

- **인물 번호화, a/s 접두사, WebP 자동 변환 및 쇼케이스 웹 템플릿 고도화** (`image-output-rules.md`, `image-assets.md`, `tools/images/`).
  - `{base}/{id}/a01.webp` (Safe/일반) 및 `{base}/{id}/s01.webp` (NSFW/성인) 표준 URL 스키마 및 시스템 프롬프트/키워드북 매핑 규격화.
  - 배포 시 모든 이미지를 `.webp` 포맷으로 자동 일괄 변환(`convert_to_webp`) 파이프라인 탑재.
  - 반응형 다크 아카데미 쇼케이스 웹 템플릿(`tools/images/web_template/`: `index.html`, `styles.css`, `app.js`) 내장 및 `--scaffold` 시 자동 주입 지원.

## 0.9.4

- **이미지 생성 프롬프트 문법 전면 제거 및 호스팅/웹 갤러리 제작 전문화** (`image-assets.md`, `docs/image-assets.md`, `SKILL.md`).
  - 생성 모델별(SDXL, Pony, Illustrious, Flux) 프롬프트 문법 및 품질 프리픽스 서술을 완전히 제거하여 스킬 본연의 역할(스토리챗 설계 및 배포)에 집중.
  - 에셋 축 설계, Cloudflare Pages 디렉터리 레이아웃, `index.html` 반응형 쇼케이스 템플릿, 배포 전 축 무결성 검증(`deploy.py --check`)을 중심으로 문서 재정립.

## 0.9.3

- **Cloudflare Pages 기반 초고속 호스팅 및 웹 쇼케이스 갤러리 일체화 지원** (`image-assets.md`, `docs/image-assets.md`, `tools/images/deploy.py`).
  - 한국(서울) 엣지 POP을 통한 0초대 로딩 속도 및 무제한 대역폭 서빙 가이드라인 수록.
  - `deploy.py --scaffold` 실행 시 작품 소개, 등장인물 도감(Roster), 일러스트 갤러리를 겸하는 반응형 웹페이지(`index.html`) 자동 생성 기능 탑재.
  - Cloudflare Pages 무료 서브도메인(`https://<project-name>.pages.dev/`) 및 커스텀 도메인 매핑 룰 명문화.

## 0.9.2

- **이미지 출력 규칙 및 스테이징 가이드라인 신설** (`image-output-rules.md`). 마크다운 이미지 호출 문법(`![]({IMG}/[카테고리]/[번호].png)`), 시스템 프롬프트 내 1줄 번호 매핑 압축법, 출력 타이밍 및 스로틀링 4대 원칙(장소 상단 1회, 인물 대사 직전 1회, 동일 인물 중복 금지, 턴당 최대 2장 제한), 키워드북 19+/특수 CG 분리 패턴 수록.

## 0.9.1

- **프롤로그 & 시작 프롬프트 페어링 설계 지침 신설** (`opening-design-guide.md`). 7단계 점진적 줌인(거시세계 ➡️ 미시무대 ➡️ 1줄 훅 ➡️ 돌발위기 ➡️ 대치+조력자 ➡️ 무선택지 압박 마감) 프롤로그 공식 및 4단 핫스타트(앵무새 방지, 100% 무마찰 수용, T1 HUD 세팅) 시작 프롬프트 가이드 구축.
- **출력 규약 및 포맷팅 가이드라인 신설** (`output-contract.md`). 지문/대사 구분 문법(`*지문*`, `이름: "대사"`), 특수 채널/통신(`💬 이름｜"대사"`), 기계음(`👾 개체｜`코드``), 미디어 호출 규칙, 안티패턴(대리서술·재인용·번호선택지 차단), 모바일 2~3문장 호흡 규약 정립.

## 0.9.0

실전 상위 랭킹 작품들의 OOC 분석 데이터와 엔지니어링 패턴을 반영하여, 스킬 레퍼런스를 전면 개편하고 순수 시스템 프롬프트 프리셋 및 상태창 가이드라인을 구축했다.

### 추가

- **100% 순수 장르별 시스템 프롬프트 프리셋 라이브러리** (`system-prompt-presets.md`). 고유명사를 완전히 배제하고 즉시 슬롯만 채워 쓸 수 있는 순수 프리셋 4종(SF/사이버펑크, 판타지명가, 현판헌터, 아카데미로맨스) 신규 수록.
- **상태창(HUD) 구성 가이드라인 및 표준 템플릿** (`status-window-guide.md`). ` ```Info ` 코드블록 8대 표준 슬롯(`[🔈]`, `[📅]`, `[📍]`, `[👤]`, `[👥]`, `[🎒/⚔️]`, `[상태/태그]`, `[상황]`), 상황/관계 이모지 체계(`🟢`, `⚔️`, `💋`, `♀🔞`, `♂🔞`, `💝`, `❤️‍🔥`), 턴 간 상태값 계승 및 1줄 객관적 사실 요약 갱신 룰 정립.
- **3단 입력 파싱 계층 및 /OOC 모드 스위칭** (`crack-prompt-rules.md`). `1순위 [ ] 메타 지시문 > 2순위 *text* 지문/설정값 > 3순위 평문 대사`의 엄격한 해석 순서와, 롤플레이 분리 Q&A `/OOC` 프로토콜 명문화.
- **위험도 기반 비대칭 선택적 압축 전략** (`semantic-compression.md`). 고위험 핵심 규칙(6중 잠금)은 단일 골든 예시를 명시하여 엄격성 유지, 저위험 스타일/나열형 규칙은 `·`, `+`, `,` 구분자로 극압축, 금지/허용 대조 1문장 통합, 채움형 문장부호 트리거(`[.,,,",*,INFO]`) 운용법 수록.
- **4대 표준 트리거 연출 모듈 키워드북 분리 규격** (`keyword-book.md`). 평상시 토큰 낭비와 일상 씬 출력 오염을 방지하기 위해 키스/스킨십, 성애(19+), 특수전투, 심화사교 연출 지침을 키워드북 항목(≤400자)으로 분리 등록하는 표준 템플릿 4종 수록.

### 변경

- **응답 분량 제어의 오프닝 앵커링 공식 전환** (`crack-prompt-rules.md`, `prompt-writing.md`). 고정 글자수/문단수 제한을 폐기하고, "세션 첫 응답(오프닝) 서술량 수준 유지 + 직전 턴 답습 금지 + 특수씬(전투/19+) 분량 급상승 클램프"로 전면 전환.
- **스토리 능동성 엔진과 6중 자율성 잠금 조화** (`crack-prompt-rules.md`, `production-patterns.md`). ⓤ의 대사·행동·생각·감정·의도 6중 잠금을 유지하되, **"ⓒ(NPC)의 선제적 개입(질문, 제안, 기습, 스킨십 등) 전면 허용"** 및 세계 자율 변화를 명시하여 스토리가 멈추지 않는 자율 구동 엔진 탑재.
- **부정편향 6대 유기적 완충망 탑재** (`crack-prompt-rules.md`, `production-patterns.md`). 단순 이분법 배제(회색지대), 건조한 실패 팩트 묘사, 인위적 숭배·악당화 양방향 과장 금지, SlowBurn 현실적 관계성, 텐션 동기화(일상 요청 시 조기 교전종결 및 복귀), 추론 상시 자기검토의 6대 완충망 정립.
- **동적 4단 말투 합성 메커니즘** (`character-personality.md`, `prompt-writing.md`). 어미 고정과 토큰 낭비를 유발하던 정적 대사 예시를 배제하고, `[성격키워드 + MBTI/성향 + 관계단계 + 상황맥락]` 4단 벡터를 조합해 매 턴 유기적으로 대사를 생성하는 원칙으로 전환.
- **1인 1줄 10대 슬롯 파이프(｜) 인물 압축 표준화** (`semantic-compression.md`). `▶이름:[성별｜소속/직책｜나이｜이명｜MBTI·에니어그램｜성격키워드｜등급/경지｜무기/능력｜외형특징｜말투/특징]` 포맷으로 10명 이상의 주연 인물을 1,000자 이내로 압축.
- **나레이터 페르소나 3단 톤 스위칭 및 메타 오류 셀프디스 수습** (`prompt-writing.md`). 드라이 유머 관찰자 시점 및 일상/전투/로맨스 톤 전환, AI 오류 발생 시 유머러스한 변명으로 자연스럽게 무마하는 안전장치 탑재.

## 0.8.5

실제 플레이테스트에서 드러난 실패를 규칙으로 옮겼다.

### 추가

- **NPC 권한 경계**(`prompt-writing.md`). 판정 권한에 범위를 안 적으면 그 장면에서 가장 목소리 큰 인물이 아무것에나 처분을 내린다. 한 플레이테스트에서 기사학부 주임이 타 학부 학생의 술식을 재단하고 등급을 보류시켰다. 소관 명시 + 가능한 결과 열거 + 실제로 측정한 것에만 판정을 묶는 세 규칙과, 경계를 강제하는 인물을 지정하라는 규칙.
- **반응 편향 균형**(`prompt-writing.md`). 조언자·라이벌을 전부 `반례 제공자`로만 규정하면 세계가 일률적으로 적대적이 된다. 금지 규칙에 진행 의무를 짝지우듯, 부정 반응에 인정·흥미·기회 제공을 하한으로 짝지운다.
- **상태창 이모지 트리거**(`keyword-book.md`). 크랙이 이모지를 키워드로 받는 것이 확인됐다. 작품이 매 턴 출력하는 상태 이모지를 트리거로 쓰면 항목 수명과 상태 수명이 일치한다. 성인 이미지 코드 145자를 통합본에서 뺀 실측 포함.
- **시스템 교체 후 잔재 청소**(`validation.md`). 폐기한 어휘가 한 곳이라도 남으면 모델이 두 설계 사이에서 진동한다. 프롤로그가 가장 자주 누락되고 가장 비싸다. 부정문(`A·B·C를 재지 않는다`)도 어휘를 가르친다는 점 포함.
- **도입 명사 검증**(`validation.md`). 플레이어에게 보이는 산문에만 등장하고 원본에 없는 명사는 즉시 세계의 물건이 된다. `check_symbols.py`는 연산자만 보므로 못 잡는다.
- **항목 병합 시 필드 소실**(`keyword-book.md`). 400자 상한 때문에 항목을 합치면 필드가 조용히 빠지고 검사기는 글자 수만 본다. 병합 후 필드별로 세는 절차.
- **채워진 예시값 복제**(`prompt-writing.md`). 상태창 예시의 이름·수치를 모델이 그대로 가져다 쓴다. 플레이어가 정하는 슬롯은 미확정 토큰으로 채운다.
- **상태창을 읽기 전용 표시가 아니라 인터페이스로 설계**(`conversation-continuity.md`). 다섯 패턴 — 코어 루프에 필드 하나 주기(직전 문단에서 유추 안 되는 값이 있어야 필드값을 함), 단일선택 슬롯과 누적 표식 구분(안 적으면 이력 표식이 감정 바뀔 때 덮어써짐), 상태 필드로 규칙 게이트하기(가시적·묘사된 사실에 조건부·표시≠동의), 필드별 시작값 선언(`미상`=아직 모름 / `없음`=비어 있음이 확정), 변형별 필드 범위(SAFE가 절대 못 채우는 필드는 매 턴 노이즈).
- **빌드 직접 편집 확인**(`SKILL.md`). 재컴파일 전에 원본보다 새로운 산출물이 있는지 확인하고 읽는다. 작성자는 실제로 빌드 파일을 고치며, 그걸 없는 일로 취급하면 작업물이 사라진다.

## 0.8.4

### 변경

- **현행 제작자 커스텀 프롬프트 계약을 명문화** (`SKILL.md`, `crack-prompt-rules.md`, README). 통합 프롬프트는 구형 일반 캐릭터 프롬프트 한도가 아니라 각 SAFE/UNSAFE 게시본별 7,000자 전체 입력란을 기준으로 검사한다. 별도 대화기록 필드·파일·`Previous History` 지시는 만들지 않는다.
- **프롬프트 조립 순서의 근거를 정리** (`prompt-writing.md`). 세션 목표·장르가 뒤 설정의 해석 기준이 되도록 앞에 두고, 실행 직전 필요한 출력 계약은 뒤에 모은다. 이를 특정 모델의 고정 가중치 법칙으로 과장하거나 같은 규칙을 반복하는 근거로 쓰지 않는다.
- **연속성 설명을 현행 구조에 맞게 수정** (`conversation-continuity.md`, `production-patterns.md`, 문서). 상태창은 공개된 현재 상태를 보여주는 선택적 출력 계약이지 저장소나 대화기록 대체 API가 아니다. 매 응답 출력은 작품이 고정 HUD를 요구할 때만 선택하며, 플랫폼의 숨은 영속성을 보장한다고 주장하지 않는다.

### 고침

- 0.8.3에서 일반화한 플레이어 프로필 필드가 `conversation-continuity.md`, `validation.md`, `docs/artifacts.md`에는 여전히 이름·나이·성별·이능명·이능 설명·희망 상황의 고정 6필드로 남아 있던 문제. 모든 검증 문구를 프로젝트가 실제로 정의한 필드 목록으로 통일했다.
- `.skill` 릴리스 자산에 로컬 `.DS_Store`가 포함되던 문제. 패키징 단계에서 루트와 하위 디렉터리의 `.DS_Store`를 제외한다.

## 0.8.3

### 변경

- **플레이어 프로필 필드를 프로젝트별로 도출** (`crack-prompt-rules.md`, `SKILL.md`). 실전 프로젝트를 만들며 발견: "이름/나이/성별/이능명/이능 설명/희망 대사"를 스킬이 고정 6필드로 못박고 있었다. 이능 없이 마법·무기·직업 기반이거나 능력 축 자체가 없는 세계관에는 맞지 않는다. story.md·characters.md가 실제로 요구하는 항목(이능/마법 계통/주 무기·기술/없음)으로 매 프로젝트가 필드를 다시 정하도록 바꾸고, "6개 항목" 하드코딩 문구를 전부 "정의한 항목"으로 일반화했다.
- **압축 순서에 한자 등 저비용 문자 체계 단계 추가** (`docs/artifacts.md`). 산문 압축 전에 시도할 단계로 삽입.

## 0.8.2

### 추가

- **UNSAFE 델타 작성 규칙 두 가지** (`content-variants.md`). 실전 프로젝트 하나를 강화하며 확인한 것: (1) "묘사할 수 있다" 식 허용문은 다큐/요약 톤에 눌려 실제로 반영 안 됨 — 명령형("묘사한다")으로 써야 매 턴 반영된다. (2) 턴 하나에 여러 해·장면을 압축하는 세계관 계약은, 강도 문구를 아무리 세게 써도 장면 단위 묘사를 조용히 요약해버린다 — UNSAFE 델타가 그 압축 규칙을 해당 턴만 명시적으로 정지시켜야 실제로 장면이 살아남는다. 실측: 턴 예외 문구 추가 비용 ~190자(7,000자 상한 대비 무시 가능).
- **UNSAFE 델타에 하드리밋 명문화 요구** (`content-variants.md`). 미성년 성적 대상화 금지·실존 인물 성적 묘사 금지·비동의 폭력 미화 금지를 빌드된 파일 안에 직접 적으라는 규칙. 이전엔 어느 레퍼런스에도 이 세 가지가 명시돼 있지 않았다 — SAFE/UNSAFE 델타를 작성할 때마다 각자 판단에 맡겨져 있었다.

### 제거

- **`!` 접두 선언 명령어.** 크랙에 단축어 기능이 생겨 우회책이 불필요해졌다. 프롬프트 안에 명령 문법을 직접 정의하면 평범한 대사가 명령으로 오인될 수 있고, 명령 목록을 상시 통합본에 실어야 하며, 플랫폼이 제공하는 것을 중복 구현하게 된다.

### 변경

- **진행 명령을 단축어가 소유한다** (`crack-prompt-rules.md`, `keyword-book.md`, `validation.md`). 국면·시대·장소를 플레이어가 선언해 바꾸는 것도 단축어로 등록한다. 진행 단축어는 일회성 작업과 달라 두 가지를 더 적는다 — **바뀌는 것과 계승되는 것**, 그리고 **모델이 스스로 국면을 넘기지 못한다**는 것.

## 0.8.0

배포되어 돌아가는 크랙 작품 두 편을 분석해 반영했다. 실측한 것만 숫자를 적었다.

### 추가

- **데이터와 규칙에 다른 문법을 쓴다** (`semantic-compression.md`). 인물·시설 같은 데이터는 조사·공백을 버리고 한자를 써서 실측 −33%, 주도권·동의 같은 경계 규칙은 문장으로 남긴다. 규칙문을 더 밀도 높은 언어로 쓰는 선택지는 **검증되지 않은 트레이드오프**로 표시했다 — 배포된 작품들이 그렇게 쓰고 작동하지만, 한국어보다 더 잘 지켜진다는 증거는 아니다.
- **Platform history summarization** (`conversation-continuity.md`). 크랙이 이전 대화를 요약하며 그 방식을 작가가 지시할 수 있는 것으로 보인다. 기본값은 생생한 세부를 남기고 지속 상태를 버려서 연속성에 정확히 반대다. 미확인이므로 요청으로 취급하고 상태창에 의존을 남긴다.
- **NPC 발언은 확정 사실이 아니다** (`conversation-continuity.md`). 모델이 뱉은 NPC 대사를 스스로 정사로 취급해 방어하는 문제. 발언을 그 인물의 주장으로 격하하고, 말실수·오해로 정정하는 경로를 주되, **그 정정을 ⓤ의 발언으로 떠넘기는 것을 금지**한다. 마지막 조항이 없으면 정정 장치가 주도권 침해가 된다.
- **번호 이미지 슬롯** (`crack-prompt-rules.md`). 이름 슬러그와의 비교표. 축이 하나면 번호가 싸고, 두 축을 곱하면 대조표가 감당이 안 된다.
- **국면별 등장 범위 게이팅** (`story-model.md`). 국면마다 등장 가능 인물을 닫힌 목록으로. 상시 프롬프트를 줄이고 자산 목록도 같이 묶는다. 선지식 금지를 짝으로 붙인다.
- **인물을 역할에 가두지 않는다** (`production-patterns.md`). 등급 체계가 있는 작품은 인물이 등급의 화신이 된다. 그리고 중립성은 **양쪽을 다** 막아야 한다 — 긍정 편향 금지만 적으면 과보정으로 냉담해진다.
- **금지요소를 장르 실패 모드로** (`story-craft.md`). 추상적 금지가 아니라 이름 붙은 전개로. 일상물이 스릴러로 미끄러지는 경로는 정해져 있다. 작품마다 다시 쓴다.

## 0.7.0

### 추가

- **진행 의무** (`prompt-writing.md`, `validation.md`, 예제 통합본). 매 응답에 최소 하나가 달라져야 하고 — ⓒ의 행동·환경·직전 결과·시간·새 정보 — 아무것도 안 달라진 응답 자체가 위반이다.

### 왜

금지는 플레이할 때마다 늘어나는데 적극 규칙은 늘지 않는다. 이 저장소도 네 번 연속 금지만 추가했고, 그 결과 예제 프롬프트의 `×` 가 41개가 됐는데 "매 응답에 무엇이 반드시 일어나야 하는가"는 한 줄도 없었다.

그 상태의 증상은 짧고 조심스러운 응답, 진행 없음, 그리고 **주도권 침해**다. 모델은 뭐라도 써야 하는데 정당한 재료가 다 막히면 울타리 없는 유일한 땅 — 플레이어의 내면과 행동 — 으로 들어간다. **내레이터를 굶기면 주도권 규칙이 있어도 주도권이 침해된다.**

### 변경

- 충돌 문장 제거 — `매 턴 사건 발생 강제×` 는 진행 의무가 생긴 뒤로는 빈 응답을 허가하는 문장이 된다.
- 예제 분량 상향: 800~1,400자 → 1,200~1,900자, 상한 1,800 → 2,400.
- 선택지 관련 3줄을 2줄로, ⓒ 지식·OOC·이능·관계의 중복 서술을 정리해 예산을 만들었다. 금지 41 → 39.

## 0.6.1

### 변경

- **결정 지점을 매 응답의 마감 형식으로 만들지 않게 했다** (`crack-prompt-rules.md`, `story-craft.md`, `validation.md`). 메뉴 금지의 부작용이 나왔다 — 번호는 사라졌는데 갈림길이 산문으로 옮겨가 `대피할 것인가, 남을 것인가` 로 맺기 시작했다. 하는 일은 같다. 내레이터가 선택지를 정리해 플레이어 앞에 세운다.
- 두 줄을 추가했다. **매 응답을 결정 지점으로 만들지 않는다**(대부분은 세계가 한 걸음 움직이고 끝난다), **갈림길을 본문에서 지목하지 않는다.** 결정 지점은 드물어야 무게가 생기고, 매 턴이 갈림길이면 어느 것도 갈림길이 아니다.
- `각 단계 사이에 ⓤ의 개입점을 남긴다` 같은 문장의 위험을 명시했다. 의도는 주도권 보장이지만 모델은 단계마다 멈추라는 지시로 읽는다. 예제에서 해당 문구를 제거했다.

## 0.6.0

### 변경

- **페이싱 규칙에 절차 예외를 추가** (`crack-prompt-rules.md`, `validation.md`). `한 응답에 주요 사건 둘 이상 시작×` 에 예외가 없으면 모델이 서류 절차에도 같은 예산을 쓴다. 실제로 측정 하나에 다섯 턴이 나갔다. 판별 기준은 ⓤ가 **받기만 하는가 정하는가** 이며, 받는 것은 장면을 따로 만들지 않는다.
- **도입부를 번호 목록이 아니라 도달 지점으로 적게 했다.** 번호 나열에 "몰아 끝내지 않는다"를 붙이면 최소 턴 수를 명령한 것이 된다. "세 번째 응답 안에 등급이 나온다"처럼 시점을 주면 모델이 시점을 목표로 삼는다.
- 예제의 시작 프롬프트를 그 형태로 다시 썼다. 예산 확보를 위해 첫 입력 처리·이미지 배치·시간 규칙의 중복을 줄였다.

## 0.5.3

### 변경

- **지문·대사 규칙에 이름 없는 화자 처리를 추가** (`crack-prompt-rules.md`, `validation.md`). 대사 형식을 `이름: "대사"` 로만 규정하면 엑스트라·군중·방송의 말은 갈 곳이 없어 지문 안으로 흡수된다. 규칙 위반이 아니라 규칙의 공백이고, 모델은 공백을 가장 가까운 형식으로 메운다. 화자 자리에 **역할**을 쓰게 하면 형식이 유지되면서 인물 명부도 오염되지 않는다.
- 일반 원칙으로 정리했다 — **형식 규칙을 만들 때는 그 형식에 안 맞는 경우를 어디로 보낼지 같이 정한다.**

## 0.5.2

### 고침

- `check_build.py` 가 `.DS_Store` 같은 OS 생성 점 파일을 산출물로 세어 실패하던 문제. `check_project_layout.py` 와 같은 규칙(점으로 시작하는 항목 무시)을 적용했다. macOS에서 폴더를 한 번 열기만 해도 검사가 깨지고 있었다.

## 0.5.1

### 변경

- **번호 선택지 목록을 기본 금지로 전환** (`story-craft.md`, `crack-prompt-rules.md`, `validation.md`). 기존 규칙은 "선택이 갈릴 때만 결정 지점"으로 **허용**만 했는데, 허용은 사실상 허가로 읽혀 몇 턴 만에 매 응답이 메뉴로 끝난다. 상황을 극화하는 것보다 나열하는 쪽이 싸기 때문이다.
- 특히 보기마다 괄호로 동기를 해설하는 형태 — `3. 아직 불가능하다고 말한다 (명예보다 생존을 우선한다)` — 를 **주도권 침해로 명시**했다. 고를 수 있는 것에 더해 그 선택의 의미까지 내레이터가 정하므로, 같은 대사를 다른 동기로 할 수 있다는 사실이 지워진다.
- 금지에 **대안을 붙였다** — ⓒ의 질문·닫히는 시간·눈앞의 변화 중 하나로 맺기. 목록을 뺀 자리를 지정하지 않으면 장면이 흐지부지 끝난다.
- 출력 순서에서 `결정 지점` 항목 제거. 순서에 남아 있으면 계속 채우려 든다.

## 0.5.0

### 제거

- **이미지 생성기 연동 전부** — `batch.py`, A1111/ComfyUI 백엔드, 목 서버 두 개. 어떤 도구로 그릴지는 제작자가 정할 일이고 생성기는 몇 달 단위로 바뀐다. 스킬이 책임질 것은 생성기와 무관하게 유지되는 것 — 프롬프트가 무엇을 요구하는지, 만들어진 파일이 그 요구와 맞는지 — 두 가지다.
- `prompts.json` 에서 생성기 설정(체크포인트·샘플러·스텝·CFG·로라·백엔드·학습셋 변주) 제거. 프롬프트와 축 목록만 남는다.
- `tools/imagegen/` → `tools/images/`. 더 이상 생성하지 않는다.

### 유지

- 폴더 골격 생성(`--scaffold`), 축 계약 검사(`--check`), 깃헙+jsDelivr 배포. 전부 생성 도구와 무관하다.
- 이미지 프롬프트 시트. 어느 도구에 붙여넣든 상관없다.
- 모델 계열별 품질 프리픽스(`quality_presets`) — 계열이 문법을 정하므로 도구가 바뀌어도 필요하다.

## 0.4.0

### 추가

- **파생 산출물 계층** (`build/assets/`) — 요약 코멘트와 이미지 프롬프트가 다섯 크랙 산출물과 함께 컴파일된다. 손으로 관리하는 에셋이 아니라 매 빌드마다 재생성되는 제작 입력이다. `build/`의 **파일**은 여전히 정확히 다섯 개이고 `assets/`가 유일하게 허용되는 하위 디렉터리다.
- **`check_freshness.py`** — 원본을 고치고 재컴파일을 잊은 상태를 잡는다. 섹션 단위 지문을 비교해 무엇이 바뀌었는지와 **어느 산출물을 다시 봐야 하는지**까지 알려준다. 이 상태는 다른 어떤 검사도 잡지 못한다 — 빌드는 전부 통과하고 올라간 프롬프트만 옛날 인물을 연기한다.
- **`check_image_assets.py`** — `characters.md` 명부와 이미지 프롬프트를 양방향 대조하고, 슬러그 형식·시드 중복·선두 태그 구별을 검사한다. 축 슬러그가 실제로 프롬프트에 실려 있는지도 본다.
- **`deploy.py --scaffold`** — 축 목록대로 빈 폴더와 배치표를 생성한다.

### 변경

- 이미지 프롬프트가 `.assets/` 에서 `build/assets/` 로 이동했다. 도구는 `--config` 또는 `$CRACK_PROMPTS` 로 경로를 받는다. 특정 작품에 묶이지 않는다.
- `validate.sh` 가 신선도 검사를 맨 앞에서 돌린다.
- CI에 신선도·배포 검사기 음성 대조 단계 추가.
- 모든 검사 스크립트가 `| head` 로 잘려도 트레이스백을 내지 않는다.

## 0.3.0

### 추가

- **키워드북 항목 4유형** (`keyword-book.md`) — 심화형·명부형·조회형·채움형. 유형이 등록 순서를 정한다.
  - **심화형**: 일반 상황엔 불필요하나 중요 장면에 세부가 필요한 경우. 키워드북의 가장 좋은 용도.
  - **명부형**: 구성원 수가 슬롯을 넘는 집합에서 이름이 창작되는 것을 막는 얇은 항목.
  - **채움형**: 문장부호·흔한 어미를 키워드로 써서 남는 슬롯을 채우는 상시 강화 항목. 맨 아래 등록, 1~2개, 없어도 성립하는 내용만.
- **정형화 압축** (`semantic-compression.md`) — 반복 어미의 기호 승격, 손익분기 계산, 내용별 압축 강도 차등.
- **규칙 지문 손실 검증** (`semantic-compression.md`, `validation.md`) — 압축 후 규칙 보존을 기계로 확인하는 방법.
- **지문·대사 형식 구분** (`crack-prompt-rules.md`) — 지문은 기울임(`*`), 대사는 평문. 굵게를 쓰면 강조 위계가 뒤집힌다.
- **외형 식별 기준** (`character-personality.md`) — 식별 포인트 하나씩, 축 분산, 능력 연동. 사진 한 장으로 구분되어야 한다.
- **`deploy.py --scaffold`** — 축 목록대로 빈 폴더와 배치표를 생성한다. 폴더마다 들어갈 정확한 파일 이름·뜻·권장 해상도를 적어두므로 그림은 넣기만 하면 된다. 다시 실행하면 채움 상태를 갱신해 진행 현황판이 된다.
- **폴더 골격은 컴파일 산출물** (`image-assets.md`, `SKILL.md`) — 축이 닫히는 순간 배치가 확정되므로 사람이 폴더 이름을 타이핑할 이유가 없다. 오타는 오류 없이 조용한 깨진 링크가 된다.
- **`tools/images/deploy.py`** — 이미지를 전용 깃헙 저장소로 배포하고 jsDelivr 주소를 만든다. 업로드 전에 축의 닫힌 목록과 실제 파일 이름이 일치하는지 검사하며, 목록에 없는 슬러그는 중단하고 없는 파일은 통과시킨다(부분 커버리지는 정상).
- **배포 검증** (`image-assets.md`) — 배포가 축 계약을 기계로 검사할 마지막 지점이라는 것, 호스팅 선택 기준(핫링크·축 표현력·캐시), 배포 후 표본 확인.
- `check_kb_slots.py` 확장 — 항목별 실제 로드율, 채움형 자동 탐지, 등록 위치 검사.
- 모든 검사 스크립트에 SIGPIPE 처리.

### 변경

- **키워드북 재정의** — 조회 사전이 아니라 조건부 프롬프트 조각. 배치 기준을 정보 종류에서 **지속성**으로 교체. 무대에 선 인물은 통합본으로, 상황별 연출 지시는 키워드북으로.
- 예제 통합 프롬프트를 정형화 압축 — 6,921 → 6,553자, 규칙 55개 전부 보존. 확보한 여백에 경제 구조·성장 체계·길드장 명부 추가.
- 슬롯 초과는 FAIL이 아닌 WARN. 밀린 항목이 문제인지는 통합본이 무엇을 들고 있느냐에 달렸고 스크립트가 알 수 없다.
- 캐릭터 템플릿에 식별 포인트 슬롯 추가.

## 0.2.0

실제 작품 하나를 처음부터 끝까지 만들어보며 드러난 것들을 반영했습니다.

### 추가

- **`references/image-assets.md`** — 이미지 자산 제작. 축 설계, 프롬프트 계층 조립, 모델 계열별 태그 방언, 배치 도구 규약, 목 서버 검증, 환경 문제 분류.
- **세계 시스템 설계** (`story-model.md`) — 등급 체계, 위협 분류, 성장 체계, 세력 경제. 인물보다 먼저 정해야 하는 층.
- **`scripts/check_symbols.py`** — 정의 없이 쓰인 기호·이모지 검출.
- **`scripts/check_kb_slots.py`** — 장면 목록 기반 3슬롯 초과 시뮬레이션.
- **민감 속성 라우팅** (`character-personality.md`) — 성적 지향 등을 UNSAFE 단독에 넣으면 변형 쌍이 깨진다는 점과 키워드북 대안.
- **외형 설계 기준** (`character-personality.md`) — 식별 포인트, 축 분산, 능력 연동. 사진 한 장으로 구분되어야 한다는 통과 기준.
- **키워드북 재정의** (`keyword-book.md`) — 조회 사전이 아니라 조건부 프롬프트 조각. 지속성 기준의 배치 재설계와 예산 재배치 절차.
- **지문·대사 형식 구분** (`crack-prompt-rules.md`) — 지문은 기울임, 대사는 평문.
- **파생 문서와 도구 검증** (`validation.md`) — 요약본 대조, 손 계산 금지, 검증 스크립트 음성 대조.

### 변경

- **글자수 예산** (`prompt-writing.md`) — 상한은 토큰이 아니라 글자수. 한국어가 영어의 약 절반이며, 프롬프트 초과 시 번역은 해법이 아님.
- **상태창은 연속성 운반체** — 저장소가 없으므로 매 응답 출력이 필수. 비었을 때 생략해도 되는 장식용 요약과 구분.
- **도달성 규칙** (`validation.md`) — 항목 제목은 주입되지 않음. 모델이 말해야 하는 고유명사는 로드되는 본문 안에 있어야 함.
- **3슬롯 검증은 시뮬레이션으로** — 항목 개별 검토로는 충돌을 찾을 수 없음. 등록 순서가 산출물의 일부.
- **메타 누출 금지** (`prompt-writing.md`) — 실제 금지어 목록을 적는 섹션 필요.
- **필수 출력 블록 배치** — 예시 구역 안에만 있으면 요구로 읽히지 않음. 자체 섹션 필요.
- 인물 명부는 표로 검토. 구성 비율은 스크립트로 계산.
- 캐릭터 템플릿에 외형 슬롯과 식별 포인트 추가.

### 고침

- 키워드북 레퍼런스의 예시 템플릿이 자체 검사기를 통과하지 못하던 문제.
- 상태창을 비었을 때 생략하라는 안내가 네 곳에 있던 문제.
